{"schema":1,"groups":[{"title":"Search & policy","sub":"how the loop explores the solution tree","fields":[{"key":"profile","label":"Profile","type":"enum","options":["default","fast","thorough"],"default":"default","help":"Preset bundle. default/fast = lean defaults; thorough = turn on multi-seed confirmation, the reward-hack/leakage/critic monitors AND gate them (trust_gate=gate), ablation refinement and the prompt cues — in one word. (Novelty is the agentic Researcher's call by default; the algorithmic novelty gates stay OFF.) A profile only fills fields you have not set yourself; any explicit knob here still wins."},{"key":"policy","label":"Policy","type":"enum","options":["greedy","evolutionary","mcts","asha","bohb"],"default":"greedy","agents":["strategist"],"help":"Search strategy: greedy, evolutionary, MCTS, ASHA, or BOHB (ASHA's schedule × surrogate proposal). ASHA is successive-halving SELECTION: each rung keeps its top ⌈n/η⌉ and promotes an unexpanded survivor, and `asha_live` can additionally stop a persistently underperforming node early. The rung is recorded as an audit receipt — per-node eval fidelity/budget is NOT scaled by rung yet (BACKLOG A1), so this allocates ATTEMPTS, not compute."},{"key":"card_driven_selection","label":"Card queue selection","type":"bool","default":true,"help":"ON by default: the Card queue owns macro-action selection — the engine works the Kanban board's selection-ready cards instead of the search policy's or the pilot agent's next action, and it WINS over Agent drives actions when both are on. The choice is pinned at run start (recorded in run_started), so a resume can never mix two selectors inside one run — editing it here only affects the next run you start. Turn it OFF for the legacy policy/pilot action path."},{"key":"speculation_depth","label":"Speculation depth","type":"int","default":-1,"help":"Live-prefetch backlog cap for Card-driven execution: how many speculative Card builds may be outstanding at once. -1 = AUTO and is the DEFAULT, resolved at startup to the settled Evaluation parallelism (one prefetch per concurrent eval lane, clamped 1-64); 0 = off; 1-64 = that exact cap. AUTO also re-resolves DOWNWARD mid-run: once the run has measured itself (at least 2 evaluated nodes and 2 build spans, read off its own log) and one evaluation cannot hide even 10% of a build, there is nothing to overlap and the depth ratchets to 0 — a ONE-WAY ratchet, and AUTO-only: a spelled depth never adapts. Every resolution, at startup and mid-run, appends a durable speculation_depth_settled event carrying the resolved integer plus the evidence, and the value in force is the LAST one the log recorded — not run_started's — so replay and a resume on a differently sized box reproduce the treatment this run actually used instead of re-deriving one from the box. AUTO settles itself back to OFF wherever a prefetch cannot pay for itself - a build whose roles call no LLM (nothing to overlap), a policy other than greedy, or a run with no id - so turning it on cannot stop those runs from starting. Takes effect only when Card queue selection is enabled — which it now is by default. No receipt required on any task: a prediction that misses is discarded BEFORE it runs, costing one Developer call and no GPU time, and its max_nodes slot is refunded. The `looplab speculation-gate` receipt is a maintainer's calibration BENCHMARK, not a licence to switch this on — you never need one."},{"key":"max_nodes","label":"Max nodes","type":"int","default":8,"agents":["strategist","boss"],"help":"Node (experiment) budget — the loop stops after this many."},{"key":"n_seeds","label":"Seeds","type":"int","default":3,"agents":["strategist"],"help":"Eval seeds per node (variance handling)."},{"key":"eval_parallel","label":"Eval parallel","type":"int","default":0,"agents":["strategist"],"help":"Concurrent evaluations inside one Run (GPU/experiment consumers). 0 = AUTO at launch: one evaluation per detected GPU, clamped to at least 1 — and 0 is the DEFAULT, so an untouched run is already as wide as the box; set 1 for strictly serial evaluation. Separate local GPU-owning Runs in the same OS-user filesystem namespace serialize through a crash-released host lease; other users, containers, and hosts need external admission. Live Strategist/operator updates settle 0 to serial width 1. Canonical setting; max_parallel remains raw-config compatibility only."},{"key":"llm_parallel","label":"LLM parallel","type":"int","default":0,"agents":["strategist"],"help":"Shared total for concurrent LLM-provider calls; its settled value also controls node-build fan-out and remains independent from eval concurrency. 0 = AUTO at launch and is the DEFAULT: leave the broker total unbounded and track the resolved eval width for builds. Only an explicit POSITIVE value caps the shared total and enables the named lanes the Strategist can reallocate at cadence. Live Strategist/operator updates settle 0 to serial width 1. Canonical setting; parallel_build remains raw-config compatibility only, and is inert at startup while this canonical field is set."},{"key":"ablate_every","label":"Ablate every","type":"int","default":0,"agents":["strategist"],"help":"Ablation-driven refinement every N improves (0 = off; greedy only)."},{"key":"archive_resolution","label":"Archive resolution","type":"float","default":1.0,"help":"Diversity-archive niche width in parameter space."},{"key":"asha_eta","label":"ASHA η (reduction)","type":"int","default":3,"help":"Successive-halving factor: keep top 1/η survivors per rung (asha policy)."},{"key":"asha_rung_nodes","label":"ASHA rung-0 width","type":"int","default":0,"help":"Width of the cheap rung-0 draft base for asha/bohb. 0 = use n_seeds (default)."},{"key":"surrogate_proposer","label":"Surrogate proposer","type":"bool","default":false,"help":"A2: BO-lite k-NN surrogate over (params→metric) proposes the next point (numeric tasks)."},{"key":"surrogate_explore","label":"Surrogate explore","type":"float","default":0.1,"help":"A2: UCB-style exploration weight for the surrogate (0 = pure exploit)."},{"key":"researcher_panel","label":"Researcher panel (K)","type":"int","default":1,"help":"E2: propose K ideas and keep the best by an empirical surrogate (not LLM-judge). 1 = off."}]},{"title":"Strategist & operators","sub":"A7 adaptive meta-control + richer proposal operators","fields":[{"key":"strategist_backend","label":"Strategist","type":"enum","options":["off","rule","llm","agent"],"default":"agent","help":"Optional meta-controller that picks policy/operators/fidelity at runtime. off = static config; rule = deterministic heuristics; llm = model-driven single call over aggregate stats; agent (default) = tool-using — reads the run, data, sibling runs, knowledge base & memory before deciding (B1 stuck-guarded). All fall back to rule."},{"key":"strategist_every","label":"Consult every","type":"int","default":3,"help":"Strategist consult cadence in created nodes (bounded, so it never thrashes)."},{"key":"merge_mode","label":"Merge mode","type":"enum","options":["auto","mean","ensemble"],"default":"auto","agents":["strategist"],"help":"A0b: auto (default) = ensemble whenever the Developer generates real code, mean otherwise; mean = legacy mean-param merge; ensemble = Developer recombines parent solutions (code-level)."},{"key":"complexity_cue","label":"Complexity cue","type":"bool","default":false,"agents":["strategist"],"help":"A0d: inject a complexity hint keyed on a branch’s breadth (few children = minimal; many = advanced)."},{"key":"ablate_code_blocks","label":"Ablate code blocks","type":"bool","default":false,"agents":["strategist"],"help":"A0a: ablate generated pipeline code blocks (MLE-STAR), not just numeric params."},{"key":"budget_aware","label":"Budget-aware proposals","type":"bool","default":false,"help":"A5: surface remaining eval budget into the proposal prompt (needs a max eval-time budget)."},{"key":"feature_engineering","label":"Feature engineering","type":"bool","default":false,"help":"I1: CAAFE-style — propose engineered features; the CV eval keeps only those that improve (tabular tasks)."},{"key":"failure_reflection","label":"Failure reflection","type":"bool","default":true,"help":"A4: feed recent failed-branch summaries into the proposal prompt so the proposer avoids repeating them."},{"key":"watchdog_reflection","label":"Watchdog reflection","type":"bool","default":true,"help":"Feed recent live-watchdog observations (train-monitor health verdicts + ASHA intermediate-rank flags) into the proposal prompt, so the proposer avoids re-proposing a configuration already seen training weakly."},{"key":"localize_faults","label":"Fault localization","type":"bool","default":false,"help":"C1: rank the repo files most relevant to a failure and surface them in the prompt (repo tasks)."},{"key":"novelty_mode","label":"Novelty check","type":"enum","options":["off","algo","llm"],"default":"llm","help":"How a fresh proposal is dedup-checked vs already-tried experiments. off = no explicit gate (the agentic Researcher's own read-the-history judgment stands). algo = deterministic param-distance + optional embedding similarity (cheap, no LLM). llm (default) = an LLM reads the real experiments and decides if it's a near-duplicate, then re-proposes if so (one extra LLM call per proposal, highest quality)."},{"key":"novelty_gate","label":"↳ algo: param gate","type":"bool","default":false,"help":"The algorithmic gate (used by novelty_mode=algo): nudge near-duplicate proposals off each other by param-space distance. (Legacy: setting this on forces algo mode.)"},{"key":"novelty_epsilon","label":"↳ algo: param ε","type":"float","default":0.05,"help":"E1: normalized param-space distance below which a proposal counts as a near-duplicate (algo mode)."},{"key":"novelty_semantic","label":"↳ algo: embedding","type":"bool","default":false,"help":"T5 (algo mode): also reject a proposal whose idea TEXT near-duplicates an existing node by embedding cosine, with one informed re-propose. For an LLM-judged check use novelty_mode=llm (the default) instead."},{"key":"novelty_semantic_threshold","label":"Semantic threshold","type":"float","default":0.92,"help":"T5: cosine similarity above which two idea texts count as duplicates (default 0.92)."},{"key":"debug_depth","label":"Debug depth","type":"int","default":2,"help":"T10: how many error-feedback repairs a failing lineage gets before abandonment (default 2; deeper debugging is a verified lever)."},{"key":"operator_bandit","label":"Operator bandit","type":"bool","default":false,"help":"P4: replace fixed merge/ablate cadences with a deterministic UCB over per-operator yield (Δmetric per eval-second). On under profile=thorough."},{"key":"digest_char_cap","label":"Digest budget (chars)","type":"int","default":0,"help":"M5: the Researcher’s always-on experiments digest budget. 0 = auto — scales with run size (60 chars/node, 1200–6000)."},{"key":"compressor_model","label":"Compressor model","type":"text","default":null,"placeholder":"(main model)","help":"D11: dedicated CHEAP model for agent-history summarization (auto-summary), so compression doesn’t pay the main model’s price. Blank = each loop’s own model."},{"key":"compressor_base_url","label":"Compressor base URL","type":"text","default":null,"placeholder":"(shared base URL)","help":"D11: endpoint for the compressor model. Blank = the shared LLM base URL."},{"key":"track_hypotheses","label":"Track hypotheses","type":"bool","default":true,"help":"P1 (on by default): ask the Researcher to state the one-line hypothesis each experiment tests, register deep-research directions as hypotheses, and track them to a verdict on the Hypotheses board. Open items are shown to the Researcher and may be payoff-ordered by foresight, so they can steer later proposals without directly re-ranking evaluated nodes."},{"key":"reflection_priors","label":"Cross-run memory (priors + lessons)","type":"bool","default":true,"help":"E4/M2/M3 (on by default): at run end distill the winner + structured LESSONS (incl. negative results — tested/abandoned hypotheses & failure themes) with a task fingerprint; at run start inject exact-task notes + fingerprint-matched lessons from SIMILAR past runs. No-op until a Memory dir is set (below)."}]},{"title":"Resilience & efficiency","sub":"crash recovery, dependency self-prep, and skipping doomed evals","fields":[{"key":"deep_repair","label":"Deep repair","type":"bool","default":true,"help":"C3: give the Developer the failure taxonomy + a “reproduce then fix” directive on debug/inline repair. On by default."},{"key":"inline_repair","label":"Inline crash repair","type":"bool","default":true,"help":"Hybrid: when a node crashes, the agent triages it (repair/abandon/reject_idea) and repairs mechanical crashes IN PLACE — no new node, no node-budget spent. On by default; off = every crash waits for a budgeted debug node."},{"key":"inline_repair_attempts","label":"↳ inline repair attempts (hard cap)","type":"int","default":12,"help":"HARD upper limit on in-place repair retries per node. The stopping decision itself is made by the crash-triage model, which sees this node's whole repair history and stops when it no longer knows what to change; this is the backstop for when it is wrong or its endpoint is degraded. 0 = unlimited (what a pre-existing run resumes with)."},{"key":"inline_repair_reasons","label":"↳ inline repair reasons","type":"list","default":["crash","timeout","oom"],"help":"Comma-separated failure reasons eligible for inline repair (crash | timeout | oom | setup | no_metric | drift). Default \"crash, timeout, oom\" — a timeout is repaired by reducing compute, an OOM by reducing memory, not abandoned. Drop \"timeout\"/\"oom\" to leave those nodes to fail."},{"key":"auto_install_deps","label":"Auto-install missing libs","type":"bool","default":true,"help":"When a node crashes purely because a KNOWN library is missing (ModuleNotFoundError — e.g. torch/xgboost/catboost), pip-install it into the eval interpreter and re-run instead of rejecting the idea. Trusted_local tier only. On by default."},{"key":"dep_install_timeout","label":"↳ install timeout (s)","type":"float","default":900.0,"help":"Per-package wall-clock budget for an auto-install. Generous default (900s) — large wheels like torch take minutes."},{"key":"proxy_scoring","label":"Proxy scoring","type":"bool","default":false,"help":"A6: early-signal score candidates to skip doomed full evals."},{"key":"proxy_kill_fraction","label":"Proxy kill fraction","type":"float","default":0.0,"help":"A6: skip the bottom fraction of candidates by proxy score (0 = never skip)."},{"key":"train_monitor","label":"Training-log monitor","type":"bool","default":true,"help":"Per-eval background observer that tails the live log of the evaluation in progress and asks the LLM for a health verdict (healthy / watch / broken — diverged loss, a silent CPU fallback, no learning). ON by default. There is no \"training stage\" the runtime marks out — health_check is passed for EVERY declared stage, the appended scorer included — so the eval's OWN resolved pipeline assigns the log roles instead: dep install (setup.log), the scorer (the LAST stage, by position, not by the name 'score') and any filename with two possible writers are never judged at all; every other stage is judged, and which one may ACT is the early-stop row below. Command-eval path with an LLM client only; the alert it appends is a fold-ignored diagnostic that cannot change lifecycle or champion by itself, but with Watchdog reflection on it becomes advice in a later Researcher prompt."},{"key":"train_monitor_kill","label":"Training monitor early-stop (kill)","type":"bool","default":true,"help":"ON by default: let the training monitor tree-kill an experiment its judge calls broken instead of burning the whole budget. FOUR independent conjuncts, every one fail-closed: (1) this switch is on; (2) a broken verdict at confidence >= Training kill confidence — a plateau is watch and is never killed, and no verdict (no LLM client, an unreachable endpoint, an unparseable answer) means no kill; (3) the log is one the eval plan can PROVE is this run's own training — the single-command eval.log, or a one-stage pipeline, which is that same shape wearing a stage name. In a MULTI-stage pipeline every stage is ADVISORY, so nothing there can kill; (4) the verdict is CONFIRMED by a second consecutive broken about the same log, where a tick that produced no parseable verdict re-arms the gate from zero. The node then fails normally with reason=monitor_broken, so replay reconstructs it from that one terminal event. Needs the training-log monitor above."},{"key":"train_monitor_kill_confidence","label":"Training kill confidence","type":"float","default":0.8,"placeholder":"0.8","help":"The judge's bar for a training-monitor kill: only a broken verdict at confidence >= this value ends the experiment. An absent, unparseable or NaN confidence never passes it. Ignored while the training monitor's early stop is off."},{"key":"asha_live","label":"ASHA live-curve watchdog","type":"bool","default":true,"help":"Read stdout_json intermediate metrics from the live command log and compare only same-resource nodes after enough finished siblings exist. stdout_regex remains advisory because its samples are not resource-keyed. On by default. On its own it only RANKS — a fold-ignored asha_rank diagnostic plus a trace span; ending an experiment additionally needs ASHA early-stop below (also on by default) AND a confident stop from its judge."},{"key":"asha_live_kill","label":"ASHA early-stop (kill)","type":"bool","default":true,"help":"ON by default, and the quantile no longer decides: past the grace window the same-resource rank test only NOMINATES a node, and an LLM judge — shown the live curve, the sibling distribution with the computed bar, the run's other live metrics and the training monitor's latest verdict — must answer stop at confidence >= ASHA kill confidence before anything is killed. No judge (no LLM client, an unreachable endpoint, an unparseable answer) means NO kill, so the watchdog degrades to advisory ranking. The judge is consulted INSIDE the rank gate, so it can only ever stop FEWER nodes than the bare quantile compare, never one that compare would have spared. Still limited to stdout_json evaluations that declare metric.resource_key, past the grace window, with ASHA min siblings finished peers at the SAME resource; a killed node fails normally with reason=asha_underperforming, and stdout_regex / unkeyed curves stay advisory."},{"key":"asha_live_kill_confidence","label":"ASHA kill confidence","type":"float","default":0.8,"placeholder":"0.8","help":"The judge's bar for an ASHA early stop: only a stop verdict at confidence >= this value kills, so THIS, not the quantile, is the knob that decides once ASHA early-stop is on. An absent, unparseable or NaN confidence never passes it. 1.0 makes a kill practically unreachable; 0.0 accepts any stop the judge returns. Ignored while ASHA early-stop is off."},{"key":"asha_live_quantile","label":"ASHA rank quantile","type":"float","default":0.5,"placeholder":"0.5","help":"The bar sits at this quantile along a WORST→BEST ordering of finished siblings' finals: 0.5 = the median; SMALLER lowers the bar toward the WORST peer (more conservative, 0.0 = only worse than the worst peer); LARGER is more aggressive. Only used when the ASHA watchdog is on."},{"key":"asha_live_min_siblings","label":"ASHA min siblings","type":"int","default":3,"placeholder":"3","help":"Minimum finished sibling nodes required before ASHA ranks at all, so it never acts on too little evidence."}]},{"title":"Budgets & confirmation","sub":"hard ceilings (blank = unbounded) + guarding seed-lucky winners","fields":[{"key":"max_seconds","label":"Max wall-clock (s)","type":"float","default":null,"placeholder":"unbounded","help":"Abort the run cleanly after this many seconds of wall-clock."},{"key":"max_eval_seconds","label":"Max eval time (s)","type":"float","default":null,"placeholder":"unbounded","agents":["strategist","boss"],"help":"Ceiling on cumulative time spent INSIDE evals (survives resume)."},{"key":"timeout","label":"Per-eval timeout (s)","type":"float","default":30.0,"agents":["researcher","strategist"],"help":"Kill a single eval after this long. Researcher = the “auto” per-node mode (it sizes heavy/neural-net experiments; this is the fallback)."},{"key":"max_eval_timeout","label":"Max agent eval timeout (s)","type":"float","default":3600.0,"help":"Hard ceiling for a Researcher-authored per-node eval_timeout after the agent_control.timeout permission gate. Default 3600s; operator-owned and never agent-adjustable."},{"key":"sweep_timeout_mult","label":"Sweep timeout ×","type":"float","default":8.0,"help":"A sweep node runs a whole grid in one process, so it gets this multiple of the per-eval timeout as its wall-clock budget (solution.py path; RepoTasks use their per-profile timeout)."},{"key":"confirm_top_k","label":"Confirm top-k","type":"int","default":0,"help":"Re-evaluate the best k nodes under multiple seeds before finishing (0 = off)."},{"key":"confirm_seeds","label":"Confirm seeds","type":"int","default":0,"help":"Seeds used in the confirmation pass."},{"key":"confirm_seed_base","label":"Confirm seed base","type":"int","default":1,"help":"D1: first confirm seed. Default 1 keeps confirm splits DISJOINT from the search’s implicit seed 0, so the confirm metric is a generalization signal (0 = legacy overlap)."},{"key":"holdout_fraction","label":"Holdout fraction","type":"float","default":0.25,"help":"D1/B6: fraction of host-held labels reserved as a FINAL holdout the search never sees; search/confirm evals score on the rest. Host-graded tasks. 0 = off."},{"key":"holdout_select","label":"Holdout selects champion","type":"bool","default":true,"help":"D1 (on by default): champion = best HOLDOUT metric among the val-top-k (the anti-validation-overfitting gate; AIRA val-test gap 15–16.6%). Off = holdout is audit-only (gap still shown)."},{"key":"holdout_top_k","label":"Holdout top-k","type":"int","default":3,"help":"D1: how many val-leaders get a holdout re-score at finish (free — re-scores existing predictions)."},{"key":"select_verifier","label":"Verifier tie-break","type":"bool","default":false,"help":"Use the calibrated result verifier only to break otherwise tied candidate metrics. This policy is pinned when a run starts so replay and resumed scoring cannot diverge."},{"key":"verifier_ci_tie","label":"Statistical verifier tie","type":"bool","default":false,"help":"Treat a conservative confidence-interval overlap as a tie eligible for verifier scoring. Requires Verifier tie-break; pinned when a run starts."},{"key":"select_verifier_samples","label":"Verifier samples","type":"int","default":3,"help":"Independent verifier samples per tied candidate (1–32). More samples reduce judgment noise at higher model cost; pinned when a run starts."}]},{"title":"LLM","sub":"the model endpoint that proposes ideas and writes code","fields":[{"key":"backend","label":"Role backend","type":"enum","options":["toy","llm"],"default":"llm","help":"Which roles do the work. llm is the DEFAULT: Researcher/Developer are the live model, and looplab run REFUSES to start when that endpoint is unreachable rather than degrading to empty fallback proposals that finish flat and report success. toy keeps Researcher/Developer offline (a deterministic optimizer) and is how you run without a model; optional embedding and Memora features may still use the configured model. Wrapping up a run that is already over (finalize, or a run/resume that only completes a pending finalization) never refuses — it warns and names the artifacts a missing model degrades."},{"key":"llm_model","label":"Model","type":"text","default":"qwen3:8b","placeholder":"qwen3:8b","help":"Model id for live Researcher/Developer roles and optional model-backed memory features."},{"key":"llm_base_url","label":"Base URL","type":"text","default":"http://localhost:11434/v1","placeholder":"http://localhost:11434/v1","help":"OpenAI-compatible endpoint (Ollama by default)."},{"key":"llm_api_key","label":"API key","type":"secret","default":null,"help":"Stored together with its normalized endpoint binding in a separate server-side secrets.json, never in a run snapshot or API response, and passed to active consumers as LOOPLAB_LLM_API_KEY with LOOPLAB_LLM_API_KEY_BASE_URL. POSIX mode 0600 is requested; Windows/shared-mount ACLs remain operator-managed. Leave blank to keep the existing stored credential material."},{"key":"llm_temperature","label":"Temperature","type":"float","default":0.6,"help":"Sampling temperature."},{"key":"llm_parser","label":"Structured parser","type":"enum","options":["tool_call","baml","outlines"],"default":"tool_call","help":"How structured ideas are parsed from the model."},{"key":"llm_guided_json","label":"Guided JSON decoding","type":"bool","default":false,"help":"H1: constrain structured calls with the endpoint schema (vLLM/SGLang guided_json). Leave off for Ollama."},{"key":"llm_reasoning","label":"Reasoning","type":"enum","options":["","off","on","low","medium","high"],"default":"high","help":"Send a thinking toggle in the request (provider-aware): Qwen3→enable_thinking, OpenAI/Ollama→reasoning_effort. Blank = server default (unchanged). Needs a reasoning-capable model."},{"key":"llm_reasoning_style","label":"Reasoning param style","type":"enum","options":["auto","qwen","effort","none"],"default":"auto","help":"Which request param shapes reasoning. auto picks qwen (enable_thinking) for qwen* models, else effort (reasoning_effort)."},{"key":"llm_stream","label":"Stream requests","type":"bool","default":true,"help":"Stream every request (SSE) so the timeout is an INTER-TOKEN idle limit — a long-but-alive generation is never cut off, only a stalled endpoint (which is detected and retried). Off = one blocking read (old per-op timeout semantics)."},{"key":"llm_timeout","label":"LLM idle timeout (s)","type":"float","default":180.0,"help":"LLM idle timeout. Stream mode: the inter-token stall limit; non-stream: bounds the whole read. Raise for endpoints with a long prefill on huge prompts; lower to fail fast on a flaky shared endpoint."},{"key":"llm_header_timeout","label":"↳ first-byte timeout (s)","type":"float","default":45.0,"help":"First-byte (response-headers) window for STREAM attempts — no headers ≈ a black-holed request, so fail over to a fresh connection fast instead of waiting the full idle timeout. Clamped to the idle timeout."},{"key":"llm_trust_env","label":"Honour proxy env","type":"bool","default":false,"help":"Honour HTTP(S)_PROXY / NO_PROXY / SSL_CERT_FILE env vars for LLM calls. Default off — the transport connects DIRECTLY (a picked-up ambient proxy often breaks internal endpoints). On when the endpoint needs a corporate proxy / custom CA."},{"key":"trace_llm_io","label":"Capture LLM I/O","type":"bool","default":true,"help":"Record each LLM prompt + completion into spans (the per-node Trace tab)."}]},{"title":"Agent loop & models","sub":"the self-driving agent loop + per-role model / endpoint overrides","fields":[{"key":"unified_agent","label":"Unified agent","type":"bool","default":true,"help":"One self-driving agent plays Researcher + Developer (+ Strategist) across stages, incl. crash triage. The per-role model fields below act as per-stage overrides (Researcher→propose, Developer→implement/repair). On by default; off = split roles."},{"key":"agent_drives_actions","label":"↳ agent drives actions","type":"bool","default":true,"help":"When Unified agent is on: let the agent pick the next macro action from a pure legal-action gate (it can never escape the pipeline). Off = the search policy still decides; the agent only fills the role slots."},{"key":"agent_max_turns","label":"Agent tool-loop turns","type":"int","default":0,"placeholder":"unlimited","help":"Max tool-call turns ANY agent (Researcher, pilot, crash-triage, run-chat Boss, deep-research, repo-scout) may take before its result is forced. Blank/0 = unlimited — the agent loops until done, never cut off mid-reasoning. Set a positive cap only to bound latency/cost."},{"key":"agent_time_budget_s","label":"Agent tool-loop time (s)","type":"float","default":0.0,"placeholder":"unlimited","help":"Wall-clock ceiling across an agent’s tool-loop turns. Blank/0 = no cap. Raise/clear this if a slow reasoning model gets cut off before it emits (replaces the old hardcoded 45s boss limit)."},{"key":"agent_emit_after","label":"Emit nudge after (turns)","type":"int","default":300,"placeholder":"300","help":"Convergence safety-net: after this many tool turns the loop NUDGES the agent once to stop investigating and emit its idea. Set generously — a focused researcher may legitimately read many files first. 0 = off."},{"key":"agent_emit_force","label":"↳ force emit at (turns)","type":"int","default":500,"placeholder":"500","help":"Hard backstop: at this many tool turns the loop FORCES the emit from what the agent gathered, so a model that never commits can’t burn the whole budget. 0 = off. Keep it above the nudge threshold."},{"key":"context_budget_chars","label":"Context budget (chars)","type":"int","default":1000000,"help":"H4: cap the agentic researcher’s tool-call history by middle-truncating stale output (0 = unbounded)."},{"key":"agent_stuck_detection","label":"Stuck detection","type":"bool","default":true,"help":"B1: the safety net that makes unlimited turns safe. Stops an agent that repeats the same call (or ping-pongs between two, or keeps hitting the same error) with no progress — forces its final emit instead of looping forever. On by default; reading different files / one long command never trips it."},{"key":"agent_stuck_repeat","label":"↳ repeat threshold","type":"int","default":4,"placeholder":"4","help":"How many identical call+result turns in a row count as “stuck” (min 2)."},{"key":"agent_stuck_alternate","label":"↳ alternate threshold","type":"int","default":4,"placeholder":"4","help":"How many ping-pong cycles between two calls count as “stuck” (min 2)."},{"key":"agent_self_plan","label":"Self-plan checklist","type":"bool","default":true,"help":"C1: give long-running agents a TodoWrite-style update_plan tool so they keep their OWN checklist; the current plan is re-surfaced every few turns to keep the goal in view across a long loop. On by default."},{"key":"agent_plan_reinject_every","label":"↳ plan re-inject every (turns)","type":"int","default":5,"placeholder":"5","help":"How often (in tool-loop turns) to re-surface the agent’s current plan as a reminder."},{"key":"agent_auto_summary","label":"Auto-summary","type":"bool","default":true,"help":"C2: when the tool-loop history grows long, LLM-summarize the stale middle instead of only truncating it. Trigger is Context budget if set, else a built-in ~120k-char high-water mark, so short loops are untouched. On by default; one extra model call per over-budget turn."},{"key":"researcher_model","label":"Researcher model","type":"text","default":null,"placeholder":"shared","help":"H3: per-role override — run the Researcher on its own model (blank = shared). Under Unified agent this is the propose-stage model."},{"key":"developer_model","label":"Developer model","type":"text","default":null,"placeholder":"shared","help":"H3: per-role override — e.g. Qwen3-Coder-30B for the Developer (blank = shared). Under Unified agent this is the default for the implement and repair stages; agent_stage_models can point them at different models."},{"key":"researcher_base_url","label":"Researcher base URL","type":"text","default":null,"placeholder":"shared","help":"H3: per-role endpoint for the Researcher (blank = shared)."},{"key":"developer_base_url","label":"Developer base URL","type":"text","default":null,"placeholder":"shared","help":"H3: per-role endpoint for the Developer (blank = shared)."},{"key":"strategist_model","label":"Strategist model","type":"text","default":null,"placeholder":"shared","help":"Per-role override — run the Strategist on its own model (blank = shared). Under Unified agent this is the strategy-stage model."},{"key":"strategist_base_url","label":"Strategist base URL","type":"text","default":null,"placeholder":"shared","help":"Per-role endpoint for the Strategist (blank = shared)."},{"key":"developer_plan_decompose","label":"Developer plan decomposition","type":"bool","default":true,"help":"C4 (repo tasks): the repo Developer first proposes an ordered plan of ATOMIC steps and runs a multi-step plan step-by-step, each in a FRESH bounded session, so a non-converging model can’t loop indefinitely. On by default (a 1-step plan == the old single pass)."},{"key":"developer_plan_min_steps","label":"↳ plan min steps","type":"int","default":2,"placeholder":"2","help":"A proposed plan with at least this many steps is executed step-by-step (a shorter plan runs as one single pass)."},{"key":"developer_plan_max_steps","label":"↳ plan max steps","type":"int","default":8,"placeholder":"8","help":"Cap on plan length so a runaway planner can’t spawn 100 steps."},{"key":"developer_session_max_turns","label":"Developer session max turns","type":"int","default":500,"placeholder":"500","help":"Hard per-session turn backstop for the repo Developer — the one write-heavy agent whose varied writes never trip stuck-detection. Bounds the plan phase, EACH step, and the single-session fallback so a model that never emits `done` fails cleanly with what it wrote."},{"key":"developer_session_time_budget_s","label":"↳ session time budget (s)","type":"float","default":1200.0,"placeholder":"1200","help":"Wall-clock ceiling per repo-Developer session (default 1200 = 20 min)."}]},{"title":"Developer agent","sub":"optional external coding agent","fields":[{"key":"developer_backend","label":"Developer backend","type":"enum","options":["default","opencode","aider","goose","continue"],"default":"default","help":"default = in-house templated/LLM developer; or drive an external CLI coding agent."},{"key":"best_of_n","label":"Best-of-N","type":"int","default":1,"help":"C2: generate N candidate implementations per node, keep the best by an execution-free reward (1 = off; in-house LLM developer only)."},{"key":"best_of_n_listwise","label":"List-wise BoN tie-break","type":"bool","default":true,"help":"D10: when the top best-of-N candidates tie on the static score, break the tie with a comparative LLM selection (+~3 pts vs pointwise). Static score stays primary."},{"key":"foresight","label":"Foresight (predict-before-execute)","type":"bool","default":true,"help":"FOREAGENT (arXiv:2601.05930): use the LLM as a world model to PREDICT — without executing — which candidate/hypothesis will score best, primed with a data report + experiment memory. Master switch; ON by default."},{"key":"foresight_panel","label":"Foresight hypothesis panel (K)","type":"int","default":2,"help":"Generate K candidate ideas per proposal and keep the one predicted best pre-execution (ranks structural/text ideas the numeric surrogate is blind to). >1 enables it (LLM backend); 1 = off."},{"key":"foresight_agentic","label":"↳ agentic foresight","type":"bool","default":true,"help":"Run the foresight ranking (hypothesis-board prioritization + K-idea pick) as a TOOL-USING loop that can pull actual experiment results / data facts before deciding, not a one-shot prediction. On by default (needs foresight + a client; falls back to the one-shot predictor)."},{"key":"llm_cache","label":"LLM response cache","type":"bool","default":false,"help":"T7: cache identical DETERMINISTIC (temperature 0) requests in-process. Never caches sampling calls (best-of-N/panel/novelty must vary). Off by default."},{"key":"agent_cmd","label":"Agent command","type":"text","default":null,"placeholder":"auto","help":"Override the external agent launcher (path / wrapper)."},{"key":"validate_agent","label":"Validate agent output","type":"bool","default":true,"help":"Audit each agent result (no-op/syntax/crash), retry with feedback, fall back to the LLM developer."},{"key":"agent_max_retries","label":"Agent retries","type":"int","default":1,"help":"Re-prompts of the agent on an invalid result."},{"key":"agent_patch_gate","label":"Patch gate","type":"bool","default":true,"help":"Run the agent in a git worktree and accept only edits within the edit-surface."},{"key":"agent_surface","label":"Edit surface","type":"list","default":["*.py"],"placeholder":"*.py","help":"Comma-separated globs the agent may edit."}]},{"title":"Safety & trust","sub":"isolation, human-in-the-loop, and audit-only monitors","fields":[{"key":"trust_mode","label":"Sandbox tier","type":"enum","options":["trusted_local","untrusted","hostile"],"default":"trusted_local","help":"trusted_local = subprocess; untrusted = Docker --network none; hostile = + gVisor (runsc) true-isolation runtime (B4+)."},{"key":"docker_image","label":"Docker image","type":"text","default":"python:3.12-slim","placeholder":"python:3.12-slim","help":"Image for the untrusted eval tier."},{"key":"eval_trust_mode","label":"Eval trust","type":"enum","options":["ratify_freeze","autonomous","ratify_freeze_drift"],"default":"ratify_freeze","help":"Trust policy for an agent-authored eval/metric adapter."},{"key":"seed_mode","label":"Repo seed mode","type":"enum","options":["auto","tracked","all"],"default":"auto","help":"RepoTask node-seeding policy (run-wide fallback; a task override wins). auto = copy git-tracked files when the editable is a git repo (skip untracked artifacts), else full copy; tracked = code-only; all = full recursive copy (legacy)."},{"key":"require_approval","label":"Require approval (HITL)","type":"bool","default":false,"help":"Pause for human approval of the final best before finishing."},{"key":"redact_output","label":"Redact output secrets","type":"bool","default":false,"help":"B3: mask credentials/high-entropy tokens in persisted event, trace and UI stdout/stderr tails. Raw node-workdir logs, source and artifacts are not scrubbed; protect the run root and avoid exposing secrets to candidate code."},{"key":"reward_hack_detect","label":"Reward-hack detector","type":"bool","default":false,"help":"B5: audit suspicious wins such as grader/answer-key access, frozen-file writes and perfect-score heuristics. Findings are advisory under trust_gate=audit; high-precision reward-hack/leakage signals can gate or block under the configured trust gate."},{"key":"code_leakage_detect","label":"Code-leakage scan","type":"bool","default":false,"help":"I3: static scan of each solution for train→test leakage (fit-before-split, fit-on-test); surfaced in the Trust panel."},{"key":"critic_check","label":"Independent critic","type":"bool","default":false,"help":"C4: execution-free critic of each solution (stub / hardcoded-metric / params-ignored); surfaced in the Trust panel."},{"key":"trust_gate","label":"Trust enforcement","type":"enum","options":["audit","gate","block"],"default":"audit","help":"T2: audit surfaces findings only; gate excludes nodes with high-precision reward-hack/leakage signals (plus critic:hardcoded_metric) from best-selection and breeding; block also marks them infeasible. Broad critic, perfect-score, audit-unavailable and suspicious-output heuristics remain advisory."}]},{"title":"Knowledge & memory","sub":"directories the scientist reads + literature/deep research","fields":[{"key":"researcher_tools","label":"Run-introspection tools","type":"bool","default":true,"help":"On (default): the Researcher (and Deep-Research) can read its own experiments (list/read/find-analogous/themes/code) and the task data (schema/profile) mid-loop. Off: legacy single-shot proposer (richer default digest still added)."},{"key":"cross_run_tools","label":"Cross-run tools (same task)","type":"bool","default":true,"help":"Give the agentic Researcher / pilot read-only tools to inspect SIBLING runs (same task_id, same run-root) — list them, read an experiment / its code, find analogous configs — so a run builds on neighbours instead of rediscovering. Advisory; on by default."},{"key":"all_runs_tools","label":"All-runs tools (every task)","type":"bool","default":true,"help":"Broader than cross-run: read-only access to EVERY run on this machine across ALL tasks (list_all_runs + read_run_code + read_run_experiment) so the agent can reuse an approach from any past experiment anywhere. Advisory; on by default."},{"key":"knowledge_dir","label":"Knowledge dir","type":"text","default":"~/.looplab/knowledge","placeholder":"unset","help":"Markdown notes for agentic retrieval (also where pre-research is saved)."},{"key":"skills_dir","label":"Skills dir","type":"text","default":null,"placeholder":"unset","help":"Recursive */SKILL.md packages plus root-level *.md skills the Researcher can list and load; for the shipped flat sample use examples/skills."},{"key":"literature_search","label":"Literature grounding","type":"bool","default":false,"help":"E3: give the agentic Researcher an arXiv search tool (network-optional; fails gracefully if blocked)."},{"key":"web_search","label":"Web search (deep research)","type":"bool","default":false,"help":"P2: give the Deep-Research stage a general web search/fetch tool on top of arXiv (network-optional)."},{"key":"deep_research_every","label":"Deep research every N nodes","type":"int","default":3,"placeholder":"3","help":"Run the Deep-Research stage automatically every N created nodes (0 = manual button / Strategist only)."},{"key":"research_verify","label":"Deep-research verifier","type":"bool","default":true,"help":"D8: check every Deep-Research memo’s CLAIMS against their cited evidence (node ids / urls) before recording — a deterministic layer always, plus an LLM rubric pass when a client is present. Verdicts cannot change this run’s nodes/champion, but finalize requires an aligned supported verdict before persisting positive D8 cross-run claim evidence. On by default; no-op with no memo/claims."},{"key":"report_every","label":"Run report every N nodes","type":"int","default":3,"placeholder":"3","help":"Regenerate the agent-authored conclusion-first run narrative every N created nodes (0 = off; it still regenerates on a manual report_refresh). The deterministic report always renders regardless."},{"key":"concurrent_research","label":"Concurrent research (overlap eval)","type":"bool","default":true,"help":"Overlap a due deep-research \"think\" with the GPU-bound eval, so the agent works while the node trains. The memo is recorded immediately when it finishes and its directions can steer the next proposal. Best with a REMOTE LLM (no GPU contention with eval); on by default — disable it if your local model contends with training."},{"key":"concurrent_research_repeat","label":"Repeat research during eval","type":"bool","default":true,"help":"Don't idle a long (multi-day) eval: re-run the overlapped deep-research on an adaptive timer for the whole training window instead of once, self-paced (skips identical re-runs, backs off as it converges). New records never rewrite the current champion, but their hints/open hypotheses can steer later proposals and replay reconstructs that advice. On by default."},{"key":"concurrent_research_interval_s","label":"Repeat research interval (s)","type":"float","default":1800.0,"placeholder":"1800","help":"Base seconds between repeated concurrent-research passes — a FLOOR: the effective pace is max(this, ~5% of the per-experiment time budget). A two-day eval re-researches about hourly; a short eval not at all. Only used when 'Repeat research' is on."},{"key":"concurrent_research_max_calls","label":"Repeat research max calls","type":"int","default":40,"placeholder":"40","help":"Per-eval-window cap on repeated-research LLM calls (0 = cadence-only). Past it the loop stops calling the LLM (the training-health monitor still runs)."},{"key":"concurrent_consolidate","label":"Consolidate board during eval","type":"bool","default":true,"help":"Dedup/merge the open hypothesis board during a long eval only for legacy policy selection. Card queue selection defers the merge to the joined between-node cadence because it changes Card ownership and readiness. Needs Track hypotheses; overlap also needs Concurrent research. On by default."},{"key":"prompt_dir","label":"Prompt dir","type":"text","default":null,"placeholder":"unset","help":"Editable, hot-reloaded role prompt .md files."},{"key":"memory_dir","label":"Memory dir","type":"text","default":"~/.looplab/memory","placeholder":"unset","help":"Cross-run case memory."},{"key":"comparative_lessons","label":"Comparative lessons","type":"bool","default":true,"help":"M6: distill credit-assigned lessons from PAIRS of solutions — which specific change made a child beat (or regress from) its parent, and what fixed a failure — on top of the run-end reflection. Gated on cross-run memory (needs a Memory dir). On by default."},{"key":"lessons_every","label":"↳ write lessons every N nodes","type":"int","default":4,"placeholder":"4","help":"M6 mid-run: every N created nodes, WRITE comparative lessons to the shared cross-run store during the run (one batched LLM call per firing). 0 = run-end write only."},{"key":"lessons_refresh_every","label":"↳ refresh lessons every N nodes","type":"int","default":4,"placeholder":"4","help":"M6 mid-run: every N created nodes, RE-READ the store so lessons distilled by CONCURRENT runs reach this run’s proposals (file re-read, no LLM call, skipped when unchanged). 0 = run-start read only."},{"key":"embed_model","label":"Embedding model","type":"text","default":null,"placeholder":"hash (lexical)","help":"T4: model for an OpenAI-compatible /embeddings endpoint (e.g. nomic-embed-text) → SEMANTIC kb_search / case retrieval. Blank = dependency-free lexical hashing. A misconfigured/offline endpoint degrades back to lexical (never crashes)."},{"key":"embed_base_url","label":"Embedding base URL","type":"text","default":null,"placeholder":"reuse llm_base_url","help":"Endpoint for embeddings if different from the chat model’s (blank = reuse llm_base_url)."},{"key":"memora","label":"Harmonic memory (Memora)","type":"bool","default":true,"help":"Index cross-run cases + knowledge notes by a short ABSTRACTION + cue anchors (not raw text), consolidate near-duplicate memories, and expand retrieval through anchors to surface related-but-not-similar memories. On by default; off restores the raw-text index."}]},{"title":"Cross-run research","sub":"experimental Part IV/V portfolio memory, retrieval, and governance","fields":[{"key":"concept_pivot","label":"Concept coverage pivot","type":"bool","default":true,"help":"Part IV: at the seed boundary and concept_retag_every cadence, record a bounded concept-coverage snapshot and, on an explore stance, name uncovered regions in the Researcher cue. This live cue can steer proposal admission and therefore affect downstream selection; it does not itself rank candidates or declare a winner."},{"key":"concept_run_base","label":"Run-base concept deltas","type":"bool","default":true,"help":"Part V: seed a run-wide concept base from the first evaluated node, then let new nodes author only additions and removals relative to that base and their parent. Folded effective axes drive Concepts, chips, grouping, and coverage only when run-base and per-node materialization receipts are complete. Partial or unavailable receipts are marked in the UI; retained IDs are display-only and never authoritative filters or groups. Replay remains the durable audit source."},{"key":"concept_retag_every","label":"Concept classifier cadence","type":"int","default":30,"help":"Part V: refresh classifier evidence, consolidation, and the concept-coverage snapshot every N created nodes. Researcher-authored additions and removals still appear immediately; this controls only the heavier classifier pass."},{"key":"graded_novelty","label":"Graded concept novelty","type":"bool","default":true,"help":"Part IV: before the ordinary novelty gate, distinguish a genuinely different implementation or a sound failed direction worth reopening. It can change proposal admission but never metric ranking; incomplete concept classification defers to the ordinary gate."},{"key":"capability_expansion","label":"Capability expansion on lock-in","type":"bool","default":false,"help":"Part IV opt-in: when Concept coverage pivot has recorded action-space lock-in and the stance is explore, direct the Researcher toward missing infrastructure or capabilities. The resulting expand proposal competes normally; enabling this does not guarantee it is built or improves the metric."},{"key":"fingerprint_universal","label":"Universal task fingerprints","type":"bool","default":true,"help":"Part IV: include Unicode words in task fingerprints so non-Latin goals are not reduced to kind/metric only. Keep one mode across a live portfolio: changing it changes which stored runs are considered similar."},{"key":"cross_run_concepts","label":"Concept capsules + prior surfacing","type":"bool","default":true,"help":"Part IV: write concept capsules and surface overlapping earlier concepts as audit-only priors. D8 memo-derived claim rows persist independently whenever a shared Memory dir is configured; this toggle does not control them. Prior surfacing also needs Concept pivot and Graded novelty."},{"key":"cross_run_structured_claims","label":"Structured claim identity","type":"bool","default":true,"help":"Part IV: use task-scoped, polarity-aware claim identity for advisory projections. Enable consistently for readers of the same portfolio; this changes grouping, not the immutable source records."},{"key":"cross_run_read_tools","label":"Cross-run read tools","type":"bool","default":true,"help":"Part V: let supported reasoning roles query prior attempts, claims, the bounded Atlas preview, and retrieval results. When a role uses these tools, the extra model/tool-loop turns and returned context can increase latency and inference cost. Applicability is a retrieval heuristic, not an authorization boundary: exact-task and bounded goal-fingerprint matching reduce but cannot eliminate irrelevant or foreign-task material. Stored portfolio text is untrusted model input. Requires a shared Memory dir."},{"key":"cross_run_advisory","label":"Cross-run prompt advisory","type":"bool","default":true,"help":"Part IV/V: inject a bounded evidence-and-counter-evidence pack into Researcher and Strategist prompts. It can steer proposals but never selects a winner directly. Scope/redaction and durable derivation receipts are still experimental; stored text is untrusted. Requires a shared Memory dir."},{"key":"cross_run_curation","label":"Taxonomy steward proposals","type":"bool","default":true,"help":"Part V: at finalization, synchronously run up to three paid proposal-only stewards for concept taxonomy, claims, and task facets. This can delay finalization and adds model/tool-loop cost before terminal cost roll-up. Proposals are recorded for operator review and never applied automatically; caught failures do not prevent terminal completion. Requires shared Memory and an LLM backend.","warningTitle":"Synchronous finalize work.","warningTone":"warning","warning":"May increase finalization latency and paid model cost even though steward failures are non-terminal and proposals remain review-only."},{"key":"cross_run_curation_auto","label":"Legacy auto-apply request (audit only)","type":"bool","default":false,"help":"Compatibility flag retained for old configurations. Finalization records that auto-apply was requested, but never applies an agent proposal; changing portfolio meaning requires an explicit operator CLI/API action, or the separate Ratify steward merges toggle below.","warningTitle":"Compatibility safeguard.","warningTone":"info","warning":"This flag never applies steward output automatically. It is retained only so existing configurations round-trip and the request remains visible in the curation audit log."},{"key":"concept_tidy","label":"Ratify steward merges","type":"bool","default":false,"help":"Part IV/V: after the taxonomy steward proposes, APPLY its already-recorded MERGE proposals at finalization — the consumer the steward has never had. Costs no extra model call (the judgement is already bought and logged) and adds no run events. Each decision is written to the same append-only concept_aliases.jsonl an operator merge uses, is applied at READ time so raw per-run tags are never rewritten, is stamped with the ratifier as its author, and is undone by one Clear alias action. Proposed SPLITs and PURGEs are never applied — they stay operator work. Requires shared Memory.","warningTitle":"An agent changes portfolio meaning without a human in the moment.","warningTone":"warning","warning":"This is the only path that lets a steward proposal become cross-run policy unattended. Every decision is individually reversible (Clear alias) and a cleared one is never re-applied, but concept identity drives retrieval, novelty grading and the atlas while it is in force."}]}],"agent_role_pills":{"researcher":{"short":"R","title":"Researcher may set this per experiment (auto)"},"strategist":{"short":"S","title":"Strategist may change this run-wide"},"boss":{"short":"B","title":"Boss (run chat) may change this run-wide"}}}