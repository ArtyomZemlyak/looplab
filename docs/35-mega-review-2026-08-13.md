# 35 · Mega-review of the 4-day window (66bc0e6..HEAD, 2026-08-09 → 2026-08-13)

**Status: REVIEW RECORD. Findings only — every item below is annotated at its site with a
`REVIEW (mega-review 2026-08-13)` comment; nothing except three stale doc/diagram strings and one
duplicated index row was changed in behavior or prose.** Method: seven parallel reviewers over the
337-file / ~42k-line window (engine, events, serve, core/runtime/adapters, agents/search/trust, ui,
docs+test-integrity), every reported finding re-verified against the working tree, and the four
medium-confidence majors additionally sent through adversarial verification with live
reproductions (`scratchpad/verify/` in the session; the repro mechanisms are restated in the site
comments). One reported major was **refuted** and is recorded below so nobody "fixes" it.

Grep for `REVIEW (mega-review 2026-08-13)` to walk every site.

---

## Confirmed, by severity

### Major — trust/fence boundaries

| Site | Finding |
|---|---|
| `runtime/read_fence.py` (template `_resolve`) | Absolute paths spelled with `//` or `/./` bypass the deny fence (returned verbatim unless they contain `..`; reproduced). Drive-letter paths make the fence a **complete no-op on Windows** while every surface reports the run as fenced. |
| `adapters/repo_task.py::entrypoint_candidates` | The `-m` scan reads the whole argv, so `python train.py -m eval` protects `eval.py`, leaves the real scorer editable, and suppresses the unprotected-scorer warning (reproduced). |
| `runtime/read_fence.py::fence_inputs` | An allow prefix that is an ANCESTOR of an editable root (run dir containing the repo) disables the whole fence silently. |

### Major — engine / events

| Site | Finding |
|---|---|
| `engine/orchestrator.py` (`_rerun_node`, the `reproposed` drop) | Propose-reset drops the superseded card **without** the `_reservation_minted_card` ownership check its sibling refusal path routes through — a card carrying another node's ATTACHED evidence is dropped, irreversibly (dropped cards refuse re-attach forever). Reproduced end-to-end; no test covers reset-over-attached. |
| `events/replay.py::_on_card_enriched` + `engine/research_cadence.py` | The enrichment journal cap (4,096) is first-come for NEW keys with no eviction, and the reconciliation writer has no gate on the omission signal — once the cap fills (adversarial log, or ~1,000 cards/nodes), an omitted enrichment re-appends an identical `card_enriched` on **every** cadence pass, forever. Reproduced: appends-per-pass 0,1,2,3,4… vs a converging control. |
| `events/traceview.py::hydrate_inputs` | `input_partial` propagation is dead in the common file-order case — a memo hit ASSIGNS the ancestor's partial flag over the accumulated one, so descendants of a lossy span render as complete (reproduced; one-token fix shape recorded at the site). |
| `search/foresight.py` (panel propose) | The panel re-binds the base researcher's already-bound card claim against a window from its own drifted cursor and NULLS valid claims — resurrecting the lost-claim/twin-card shape this window's card fixes closed (windows reproduced diverging at attempts 3 vs 1 over 7 cards). |

### Major — serve / UI

| Site | Finding |
|---|---|
| `serve/run_commands.py` (`PAUSE_OBSERVATION_MAX_EXTENSIONS`) | All 12 pause-observation extensions burn in the first seconds (consumed per 0.05 s monitor pass, no nearness gate), so the effective bound is ~20 min, not the "~4h" the constant claims — a pause over a long eval still terminalizes `timed_out`. |
| `serve/routers/org.py` (deletion cascade) | The cascade identity (`run_uid` + per-run `memory_dir`) is never persisted (closed receipt schema); any deletion completing on a retry after the directory is gone loses the uid permanently, and the error message points at receipt fields that never existed. |
| `serve/memory_cascade.py::RunIdentity.owns` | Deliberate legacy fallback, broken disclosure: uid-less rows are deleted by bare directory-name match even under a uid-keyed caller, while the receipt asserts `identity: "run_uid"` — cross-checkout data loss in a shared store, reproduced, and the misleading label is test-pinned. |
| `ui/src/CardBoard.jsx` | `expectedGeneration={state?.generation}` is **always null** (the folded state has no `generation`; the real value is the dropped `runGeneration` prop) — every trace surface off the board runs unfenced, and the test stub masks it by putting `generation` inside its state object. The two `/cards/{id}/trace` reads also skip `expected_generation`/`traceGenerationMatches` entirely. |
| `ui/src/Inspector.jsx` (TraceSurface) | A failed sole paged read renders the positive claim "No observations were recorded…" (+ "showing confirmed spans" with zero confirmed) for op-trace/historical-attempt subjects — permanent, since the span path has no retry budget. |

### Docs / diagram (fixed in place, with review notes)

* `docs/00-INDEX.md` — duplicated doc-34 row (merge artifact) removed.
* `docs/infographic/agent-architecture.html` — "ALL EIGHT FAILURE_REASONS" / "eight failure
  reasons" corrected to eleven; "3 consecutive node_failed" corrected to DISTINCT/cumulative.
* `docs/guide/concepts.md` — the same "eight" list corrected to eleven.
* `docs/32` — marked that its recommended fix (`declaration_actually_corrected`) landed BEFORE the
  doc did (`7e9a9a2`); §2's race is closed at HEAD.
* `docs/29` — F1e marked **BUILT** (its sibling F1b already carried the convention).

### Minor (annotated at site; see the grep)

Engine: `metric_salvage` — `diverged` missing from `NEVER_SALVAGED_REASONS` (exit-0 divergence
race is salvageable); `recheckable_expect`'s repair-written guard covers only the CHANGED set while
the commit path re-materializes all `node.files` (latent, unreachable today);
`evaluate._repair_salvaged_cause` — the "NEVER raises" tail is unguarded (ENOSPC there = node with
no terminal); `evaluate._salvage_qualifying_gates` — outer except fails OPEN (select-mode champion
with constraints never evaluated); `lessons_priors` — every legacy meta-note is silently invisible
to the E4 prior (fail-closed polarity scope, no health disclosure). Config/`cards.py` — the
statement cap is 4,000 at the boundary but 2,048 at two engine producer sites; the legacy-map
comment said "all eight". Serve: live-run `/memory-purge` has no quiescence fence; evidence-ref
namespace mixing in `lesson_keep_reason`; unlocked governance-read window in the purge;
`_reelect_active_cases` mutates untouched groups; `_intent_spent`'s finished-run exception
unreachable for pauses; `appstate` keys a scan on a bare `"node_created"` literal; the trace-route
lifecycle CAS pays two full folds per poll on live runs. Events: `project_card_trace` is unbounded
with a receipt that hardcodes `visible == total`; `_on_hypothesis_added` drops legacy over-cap rows
(fold delta on old logs); `canon_at` made `hypothesis_merged`'s splice position load-bearing
(registry comment not moved); `span_index._load_persisted` ignores its `source_handle`. Agents:
bare literals beside `BOARD_PROMPT_CARDS`; the deep-research brief's prefix escapes its own 32k
trial; the deep-research hint filter misses pre-2026-08-08 legacy rows; `critic` leaf fallback is
near-blind for ubiquitous ML tokens (executed counterexample; advisory-only). UI: drawer Escape
bypasses `DIALOG_PRIORITY`; `recoverCardControl` and `toolActivityProjection` are house-pattern
violations (plus an `<ol start>` numbering defect); bulk delete stops the batch on a `pending`
receipt; `contextUsage` dropped the `tokens.prompt` fallback; the memory-purge retry button is
offered with an empty identity (unwinnable 400 loop); the Concepts view eagerly fetches
`/api/memory` per mount with no retry. Tests: `test_card_stage_1c`'s "order_tolerant" name now
asserts order-dependence; the systemic-failure stop has no tier-1 end-to-end test; one
comment-satisfiable positive pin in `test_inline_repair_reason_coverage`; the settings-tripwire
narration stops four fields short of its own literals. Runtime: NUL paths reported as "empty".
Adapters: `protect_entrypoint` tightens resumed pre-field runs with no snapshot grandfathering.

---

## Refuted — do not "fix"

* **`engine/card_reservation.py` planner/commit fence (claimed foreign-card rebind).** Refuted on
  two independent grounds: (1) no concurrent lane can mint a card event — all four `EV_CARD_ADDED`
  writers are engine main-task, card events are excluded from every background-appendable registry,
  and serve may append only `CONTROL_EVENTS`; (2) even with the interleaving forced, the commit
  writes `plan.card_id`/`plan.idea`, never the stale linked spelling — nothing foreign becomes
  durable. The `retry_attach=True` flow is also narrower than claimed (serial build spine only).

## Verified clean (sampled highlights)

`core/fence.py` prefetch never decides; every changed `docs/guide/configuration.md` default is
correct against a live `Settings()`; all registries (READER_PATH_KEYS, TASK_OPTIONAL_HOOKS,
LOOP_OPTION_FIELDS partition, hint attrs) reconcile two-way; `speculation_quality` receipt
derivation unchanged over the shipped corpus; layering and the agents→search import direction hold;
no XSS in the new UI surfaces; every new HTTP contract moved its tests in the same window; the
memory-cascade path-traversal surface is membership-guarded and its locking matches every writer's
spelling; `metric_salvage` honors invariants #2/#3 (one terminal, durable gating).
