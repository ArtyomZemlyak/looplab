# LoopLab — guide for coding agents

LoopLab is an autonomous ML/DS research engine: a Researcher proposes ideas, a Developer writes
code, a sandbox runs it, an evaluator scores it, and the loop refines/merges the best candidates.
The append-only event log (`events.jsonl`) is authoritative for replayable `RunState`; task/config
snapshots, traces, chat and cross-run stores are explicit sidecars and are not rebuilt by replay.
Design docs live in `docs/` (see `docs/02-architecture.md`, ADRs in
`docs/03-decisions.md`).

## Commands

```bash
pip install -e ".[dev,ui]"        # dev deps; [ui] needed for server/assistant/TUI tests (fastapi)
python -m pytest                  # full suite (8,900+ collected tests, a few minutes; addopts already has -q)
python -m pytest tests/test_events_replay.py           # targeted run — always do this first
python -m pytest -o addopts="" -q ...                  # if you need to override the default -q
python -m pytest -m "not docker"  # skip Docker-daemon tests
looplab run --no-genesis --kind quadratic --goal "min (x-3)^2" --direction min --backend toy --out runs/demo  # offline smoke
# (--no-genesis matters: any --goal otherwise invokes Genesis, which needs a reachable LLM.
#  --backend toy is now REQUIRED for an offline run: the `backend` default was changed from
#  "toy" to "llm" on 2026-08-04, so without it this command hits the LLM endpoint preflight.)
looplab replay runs/demo          # rebuild state from the event log (reproducibility check)
looplab timings runs/demo         # wall-clock: per node + run-level (from spans.jsonl), reconciled
                                  # against the run's duration (events.jsonl first->last ts), residual named
looplab ui                        # FastAPI server + React UI (see looplab/serve/)
```

The suite runs fully offline in ~1-2 minutes; live-LLM tests auto-skip (opt in with
`LOOPLAB_LIVE_SCENARIOS=1`). There is no lint/format config (no ruff/black); match the style of
surrounding code (~100-col lines, heavy why-comments) and do not reformat.
Docs are built with `mkdocs build --strict` in CI — broken doc links fail the deploy.
`looplab build-ui` builds the React UI (`npm ci && npm run build` in `ui/`); `looplab ui`
auto-builds when the dist is missing. `looplab/cli/` is a PACKAGE (command groups in
`run_cmds`/`export_cmds`/`inspect_cmds`/`concept_cmds`/`governance_cmds`/`ui_cmds` — `inspect_cmds`
is run diagnostics ONLY, the Part IV concept/novelty diagnostics are `concept_cmds`, and everything
that WRITES cross-run memory or spends money on a steward is `governance_cmds`; the Typer app +
patchable builders (including the shared `_make_llm_client`/`_settings_for_run`) live in
its `__init__`; `python -m looplab.cli` works via `__main__.py`).

**Keep the docs and the process diagram in sync with the code — in the SAME change.** When you
change a default, a cadence/threshold, an event type, or add/rename a subsystem, update: (1) the
settings table in `docs/guide/configuration.md` (every `Settings` field must have a row with the
CORRECT default) and the relevant `docs/guide/*.md` page; (2) the **full process diagram**
`docs/infographic/agent-architecture.html` — a self-contained boxes-and-arrows flowchart whose
numbers/cadences/thresholds are verified against `looplab/` (embedded on `docs/guide/architecture.md`).
Stale docs/diagram are treated as a bug. The diagram is data-driven (a `B` block map + `E` edge list
in its inline `<script>`); edit the data, not hand-placed SVG.

## Package map (what lives where)

| Path | Contents |
|---|---|
| `looplab/core/` | foundation: domain models (`models.py` — `Idea`/`Node`/`RunState`; card IDENTITY split out into `cards.py`: the versioned idea/card action digests, the three ownership-receipt constructors, the footprint + steering-context vocabularies they bind, and `Card` with its provenance family, ALL re-exported through `models.py` so both spellings name the SAME objects), `Settings` (config.py = schema, appconfig.py = loader), LLM client (`llm.py` + its `llm_streaming`/`llm_toolcall`/`llm_transient` siblings — every split name re-exported through `llm.py`), `llm_broker.py` (run-local concurrency ADMISSION for outbound provider calls — a closed lane vocabulary selected through a `ContextVar`, borrowed by both clients around the real request only, so a producer can be scoped without holding capacity while it reads files or runs tools), parsing, tracing, shared errors; `pathsafe.py` (the single spelling of `is_reparse` / `WINDOWS_RESERVED` / `filesystem_identity`) and `atomicio.file_identity` (the canonical same-file-unchanged stat tuple) — a site needing a SUBSET of either documents why against the shared definition rather than re-deriving it; `jsonutil.py` (`canonical_json` + `canonical_json_digest`/`DIGEST_TEXT_CAP` — the ONE dump/hash tail every identity minter shares, preimages stay at their call sites), `setup_identity.py` (the two run-start digests `Engine._setup_phase` stamps — `run_started.config_hash` dumps the task payload UNSORTED, `setup_finished.manifest` hashes a SORTED inner config hash beside the workspace/provenance; `search/speculation_quality.py` re-derives both to prove calibration evidence came from the shipped writer, so the derivation lives here rather than being hand-copied) and `numeric.py` (`numeric_params`/`knn_idw`, re-exported from `events/digest.py`); `jsonlio.py` (the GENERIC JSONL store I/O — lenient read + health, region scan, atomic write/rewrite — moved down out of `events/` so a subsystem that only wants to read a JSONL file need not import `events`; `events/eventstore.py` re-exports the whole surface, so both spellings name the SAME objects and every existing monkeypatch seam through `looplab.events.eventstore` still works); `trace_files.py` (the descriptor-first filesystem boundary for the run-private trace sidecars — the `O_NOFOLLOW|O_NONBLOCK` open with its lstat/fstat identity CAS, `assert_private_trace_file`, and `iter_bounded_trace_jsonl_lines`, the bounded row scanner whose over-ceiling stop is the SPECIFIED append-log prefix rule, not a truncation bug — shared by the exporter, every trace reader and the destructive filter, so a hardening lands once) and its sibling `trace_append.py` (the `spans.jsonl` append-receipt contract); `memory_window.py` (`MEMORY_SOURCE_BYTES`/`_ROWS`/`_ROW_BYTES` — the ONE bounded-tail snapshot rule for the cross-run JSONL stores. `serve/routers/misc.py` used 2 MiB/1000 and `tools/memory_tools.py` 1 MiB/1024, so the Researcher's priors and the operator's view disagreed about what the store contained. Note the returned index is WINDOW-relative, never an on-disk line number — a caller that persists or compares it across reads is wrong) |
| `looplab/events/` | event store, `types.py` (event-type registry), `replay.py::fold` (event log → `RunState`), digest, readmodel, exporters + the pure UI projections (`traceview.py` — which also owns the two knobs a bounded node trace is read through: `settle_node_span_cap` (how MUCH) and `settle_trace_anchor` (WHERE), plus `node_episodes`, the map of a node's own bands that makes the second one usable. `node_episodes` IS `_conversation_bands` over the light index rather than a second reading of "what are this node's steps" — a map that derives its own answer is how it ends up naming things the surface it maps does not have — and the band `anchor` it publishes is the band's OPERATION span, because spans.jsonl is written on CLOSE and an operation's row therefore lands after everything it contains), `htmlview.py`, `comment_projection.py` = threaded-comment lifecycle projection, `belief_projection.py` = the BELIEF view over the card board — derived, never folded state), `card_ledger.py` (the derived Card ledger split out of `replay.py` — the fold-time `_bounded_card_*` receipt bounds its `card_*` handlers admit through, plus `derive_cards(st)`, the once-per-fold post-pass `_finalize_fold` runs over already-folded state; a LEAF that imports only `core`, so `replay` imports it and never the other way round, and only the names replay's own handlers CALL are re-exported — a re-export of a helper the ledger calls internally would look like a patch seam while a monkeypatch through it missed the fold), digest, readmodel, exporters + the pure UI projections (`traceview.py`, `htmlview.py`, `comment_projection.py` = threaded-comment lifecycle projection, `belief_projection.py` = the BELIEF view over the card board, `authoring_projection.py` = the IN-FLIGHT card-build head the fold cannot express as a `Card.status` at all — folding the build REQUEST would make the servicer of that very head unable to claim it, so the board called a 2,128-second build "not started" for all but 0.3 s of it — all derived, never folded state), `span_index.py` (light span index → `spans.index.jsonl`; its `_anchored` is the ONE rule for `?before=`, the trace window's SEEK — the routes' windows have always been a TAIL, so widening was the same tail extended and the beginning of a long node was unreachable at any limit, measured 74 % of rubert-dr-0804 node 1 even at the ceiling. Note its rule is the OPPOSITE of `full_spans_for_trace`'s anchor: an unplaceable id there is a just-appended span that follows every indexed row and must keep the whole prefix, while here it must be REFUSED at the boundary via `has_span`, because degrading a node window to the tail publishes the newest spans under an older episode's label. `before` is also material in `_node_window_snapshot`'s revision — two anchors at one window would otherwise mint the same validator — while `total` deliberately stays the node's FULL row count, since the omission receipt owes the operator the size of the node and not of the part behind their anchor), `notebook.py` (champion-notebook renderer, an export not a runtime), and the two finalization modules `search` may import DOWNWARD but the engine owns the writing of: `finalize_scope.py` (the finalize-SCOPE read side) and `finalize_protocol.py` (the step-name vocabulary, the `budget` receipt constructor + its exact field set, and `QUIET_FINALIZATION_SUFFIX` — the ordered event suffix a finalization with nothing optional configured emits; `engine/finalize.py` writes it and `search/speculation_quality.py` refuses calibration evidence that does not match it, so a second hand-synced copy breaks the gate silently) |
| `looplab/runtime/` | sandboxes (subprocess/Docker tiers), command evaluation, dep install, background tasks — process execution ONLY, and it imports nothing above `core`. Also `read_fence.py`, the source-tree READ FENCE: a generated per-run `sitecustomize.py` that `run_argv` puts first on a fenced eval's `PYTHONPATH`, installing a `sys.addaudithook` that REFUSES any `open` under an editable SOURCE root. It exists because `expect` checks what a stage WRITES and never what it READS — `runs/rubertlite-dr-unified-v6` node 4 trained a good model (train.log `RECALL@100: 0.726`) and then scored a HUMAN's checkpoint that an absolute path in an editable config named (`score.log: 0.225`, the number the run recorded), with the artifact contract PASSED, no `expect_failed`, no salvage, no violation. The prompt/advisory rung is SPENT: that exact note fires on `edit_file` and is in node 4's `spans.jsonl` verbatim. Deny is the default (`Settings.read_fence`) because warn would have logged this and still recorded the corrupt metric; the reasoning is in the field's own comment. The refusal is a plain non-`OSError` exception on purpose — `except OSError: <fall back>` is THE shape around a file read and would turn it into a silent skip. It never fences the workdir, the run dir, `/tmp`, site-packages, the model cache or a `data:`/`references:` mount SOURCE (allow-listed even inside an editable tree); on the READ path it is a PATH fence, not an inode one, because `realpath` per open measured +88 % against the prefix compare's +2.8 % (re-measured 2026-08-13: +254 %, and ~474 us/call on the geesefs mount workdirs live on). **`open` is not the only way to touch a file**, and the 2026-08-13 audit (`docs/38-fence-coverage-audit-2026-08-13.md`) is the map of what is and is not covered. The hook also watches `os.chdir` and the twelve MUTATION events registered in `MUTATION_EVENTS` — `os.remove`, `os.rename`, `os.truncate`, `os.chmod` and their family raise their OWN audit events and none of them raises `open`, so a node's eval code could DELETE or rename the operator's editable tree while every read of it was refused (14/14 mutation probes THROUGH, `shutil.rmtree(<the source root>)` included, because the root's own name carries no trailing separator and rmtree then unlinks each file with `os.remove(<bare name>, dir_fd=…)`, which CPython audits with the RELATIVE name). `MUTATION_EVENTS` maps event -> (path slot, dir_fd slot) and `tests/test_read_fence.py` RE-DERIVES it from a recording audit hook in a subprocess, so an interpreter that moves an argument goes red instead of leaving the fence checking a slot that is no longer a path. The rare events (chdir + mutation) resolve symlinks and descriptors — memoized `realpath` per directory plus `/proc/self/fd` — and `open` still never does; that asymmetry is the design, and without it `os.chdir(<a workdir symlink into the source>)` was permitted, which made the relative-path fast bail's own justification false. Cost of the mutation half, measured: +34 ns/open (inside the noise — a training process raises no audited event but `open`) and +580 ns per legal create/remove pair in the node's own workdir. **No CPython audit hook can see a NATIVE reader** (`safetensors`, `h5py`, `pyarrow`, `ctypes`), a non-Python child, a `python -S`/`-E`/`-I` child, a child with `PYTHONPATH` stripped, or `os.open(dir_fd=…)` (the `open` event carries no dir_fd) — that needs a KERNEL boundary, and doc 38 recommends landlock (ABI 2, verified available on this box) ALONGSIDE the hook rather than instead of it, because a kernel refusal can only be `EACCES`, i.e. an `OSError`, i.e. the silent skip this fence's exception type exists to avoid |
| `looplab/tools/` | agent-facing tools; `_base.py` documents the ToolProvider contract and owns the shared bounded-output rules (`clip`, `fit_rows`, and `stream_tails` — the per-stream tail split `run_command` and `run_probe` both report through, so the two surfaces cannot come to disagree about which half of a failure survives `RESULT_CAP`); `env_inspect.py` (repo Developer's read-only env inspector: pkg version/API/source) and its runtime twin `dev_probe.py` (F2 — the Developer's PROBE: `run_probe(code)` runs a short PYTHON program against the REAL environment while it authors, because a Developer that cannot check something works AROUND it — the observed shape was *"Since I have no shell/install ability, the cleanest repair is a small loguru shim module"*, a FAKE library written rather than one line finding out the real one imports. It is deliberately NOT a shell: `runtime/read_fence.py` is a CPython audit hook, so it covers `open` inside an interpreter and nothing else, and a tool that could run `cat`/`cp`/`bash` would be an execution surface the fence cannot see — one `cp <source>/final/model.safetensors ./ckpt` from laundering a human's artifact into a node's workspace, i.e. the v6 node 4 defect on purpose. **The probe surface IS the interpreter, because the interpreter is what the fence can cover** — a boundary, not the allow-list of commands doc 36 rejects. Four rules, all universal: no read of the editable source tree (a fence rendered from the SAME `read_fence.fence_inputs`/`render` the engine installs, ALWAYS `deny` — `read_fence=off` must not open a second door); no write ANYWHERE, where the audit hook covers a file's EXISTENCE (create/truncate/unlink/rename, with the actionable message) and `RLIMIT_FSIZE 0` covers its CONTENT in the kernel for anything the hook cannot see — measured, neither is a superset of the other, and it is these two, not a check for the word `pip`, that close the mid-run `pip install` that corrupted a RUNNING node's site-packages; no new PROGRAM (a fork inherits the hook, an exec REPLACES it, which is what makes the read fence total here); and no GPU (`CUDA_VISIBLE_DEVICES=""`, so a probe can never allocate on a device a sibling node's training holds). Its whole world is a `mkdtemp` holding a disposable replica of the node's STAGED files, deleted when the call returns — which is why it needs NO domain event: the four rules are exactly the statement that it has no side effect, so invariant #3 has nothing to gate and it is a `tool` span like every other Developer tool call. That is one decision with the read-only one, not two: a probe that could write its own workspace would make `node_created.files` stop being the whole record of what the Developer built. It joins at `adapters/repo_developer.py::_scout_tools`, the ONE point all four phases compose, and the prompt half matters as much as the tool — `_REPO_DEV_SYSTEM_BODY`'s "you CANNOT execute anything yourself" clause is now two alternatives spliced at the SAME position (`_system_body`), so `developer_probe=false` restores the old prompt byte-for-byte); `vectorstore.py` + `memora.py` (embeddings + harmonic index) |
| `looplab/core/` | foundation: domain models (`models.py` — `Idea`/`Node`/`RunState`; card IDENTITY split out into `cards.py`: the versioned idea/card action digests, the three ownership-receipt constructors, the footprint + steering-context vocabularies they bind, and `Card` with its provenance family, ALL re-exported through `models.py` so both spellings name the SAME objects), `Settings` (config.py = schema, appconfig.py = loader), LLM client (`llm.py` + its `llm_streaming`/`llm_toolcall`/`llm_transient` siblings — every split name re-exported through `llm.py`), `llm_broker.py` (run-local concurrency ADMISSION for outbound provider calls — a closed lane vocabulary selected through a `ContextVar`, borrowed by both clients around the real request only, so a producer can be scoped without holding capacity while it reads files or runs tools), parsing, tracing, shared errors; `pathsafe.py` (the single spelling of `is_reparse` / `WINDOWS_RESERVED` / `filesystem_identity`) and `atomicio.file_identity` (the canonical same-file-unchanged stat tuple) — a site needing a SUBSET of either documents why against the shared definition rather than re-deriving it; `envsafe.py` (the ONE home for "is this variable a secret?" and for the DECLARED ENVIRONMENT rule `validate_env_map`/`merge_env`. It is in `core` because the three declarers that must accept exactly the same thing straddle the layering line — `config.py::Settings.eval_env` (run level), `adapters/repo_task.py::EvalSpec.env` (task level) and `runtime/command_eval.py::validate_stages` (stage level) — and three levels that get MERGED cannot each have their own idea of a legal declaration. `SECRET_ENV`/`is_secret_env` MOVED here from `runtime/sandbox.py`, which re-exports both, so all eight importers and `tests/test_secret_env_pattern.py` still name the SAME objects; `config._SECRET_ENV_NAME` stays a separate stricter pattern because it answers a different question (what an `api_key_env` may be NAMED). A secret-shaped declaration is REFUSED rather than redacted: a declared env is written verbatim into `task.snapshot.json`/`config.snapshot.json`/`run_started`, and LoopLab deliberately has no secret store to route it to), `jsonutil.py` (`canonical_json` + `canonical_json_digest`/`DIGEST_TEXT_CAP` — the ONE dump/hash tail every identity minter shares, preimages stay at their call sites), `setup_identity.py` (the two run-start digests `Engine._setup_phase` stamps — `run_started.config_hash` dumps the task payload UNSORTED, `setup_finished.manifest` hashes a SORTED inner config hash beside the workspace/provenance; `search/speculation_quality.py` re-derives both to prove calibration evidence came from the shipped writer, so the derivation lives here rather than being hand-copied) and `numeric.py` (`numeric_params`/`knn_idw`, re-exported from `events/digest.py`); `jsonlio.py` (the GENERIC JSONL store I/O — lenient read + health, region scan, atomic write/rewrite — moved down out of `events/` so a subsystem that only wants to read a JSONL file need not import `events`; `events/eventstore.py` re-exports the whole surface, so both spellings name the SAME objects and every existing monkeypatch seam through `looplab.events.eventstore` still works); `trace_files.py` (the descriptor-first filesystem boundary for the run-private trace sidecars — the `O_NOFOLLOW|O_NONBLOCK` open with its lstat/fstat identity CAS, `assert_private_trace_file`, and `iter_bounded_trace_jsonl_lines`, the bounded row scanner whose over-ceiling stop is the SPECIFIED append-log prefix rule, not a truncation bug — shared by the exporter, every trace reader and the destructive filter, so a hardening lands once) and its sibling `trace_append.py` (the `spans.jsonl` append-receipt contract); `memory_window.py` (`MEMORY_SOURCE_BYTES`/`_ROWS`/`_ROW_BYTES` — the ONE bounded-tail snapshot rule for the cross-run JSONL stores. `serve/routers/misc.py` used 2 MiB/1000 and `tools/memory_tools.py` 1 MiB/1024, so the Researcher's priors and the operator's view disagreed about what the store contained. Note the returned index is WINDOW-relative, never an on-disk line number — a caller that persists or compares it across reads is wrong) |
| `looplab/events/` | event store, `types.py` (event-type registry), `replay.py::fold` (event log → `RunState`), digest, readmodel, exporters + the pure UI projections (`traceview.py`, `htmlview.py`, `comment_projection.py` = threaded-comment lifecycle projection, `belief_projection.py` = the BELIEF view over the card board — derived, never folded state), `card_ledger.py` (the derived Card ledger split out of `replay.py` — the fold-time `_bounded_card_*` receipt bounds its `card_*` handlers admit through, plus `derive_cards(st)`, the once-per-fold post-pass `_finalize_fold` runs over already-folded state; a LEAF that imports only `core`, so `replay` imports it and never the other way round, and only the names replay's own handlers CALL are re-exported — a re-export of a helper the ledger calls internally would look like a patch seam while a monkeypatch through it missed the fold), digest, readmodel, exporters + the pure UI projections (`traceview.py`, `htmlview.py`, `comment_projection.py` = threaded-comment lifecycle projection, `belief_projection.py` = the BELIEF view over the card board, `authoring_projection.py` = the IN-FLIGHT card-build head the fold cannot express as a `Card.status` at all — folding the build REQUEST would make the servicer of that very head unable to claim it, so the board called a 2,128-second build "not started" for all but 0.3 s of it — all derived, never folded state), `span_index.py` (light span index → `spans.index.jsonl`), `notebook.py` (champion-notebook renderer, an export not a runtime), and the two finalization modules `search` may import DOWNWARD but the engine owns the writing of: `finalize_scope.py` (the finalize-SCOPE read side) and `finalize_protocol.py` (the step-name vocabulary, the `budget` receipt constructor + its exact field set, and `QUIET_FINALIZATION_SUFFIX` — the ordered event suffix a finalization with nothing optional configured emits; `engine/finalize.py` writes it and `search/speculation_quality.py` refuses calibration evidence that does not match it, so a second hand-synced copy breaks the gate silently) |
| `looplab/runtime/` | sandboxes (subprocess/Docker tiers), command evaluation, dep install, background tasks — process execution ONLY, and it imports nothing above `core`. Also `read_fence.py`, the source-tree READ FENCE: a generated per-run `sitecustomize.py` that `run_argv` puts first on a fenced eval's `PYTHONPATH`, installing a `sys.addaudithook` that REFUSES any `open` under an editable SOURCE root. It exists because `expect` checks what a stage WRITES and never what it READS — `runs/rubertlite-dr-unified-v6` node 4 trained a good model (train.log `RECALL@100: 0.726`) and then scored a HUMAN's checkpoint that an absolute path in an editable config named (`score.log: 0.225`, the number the run recorded), with the artifact contract PASSED, no `expect_failed`, no salvage, no violation. The prompt/advisory rung is SPENT: that exact note fires on `edit_file` and is in node 4's `spans.jsonl` verbatim. Deny is the default (`Settings.read_fence`) because warn would have logged this and still recorded the corrupt metric; the reasoning is in the field's own comment. The refusal is a plain non-`OSError` exception on purpose — `except OSError: <fall back>` is THE shape around a file read and would turn it into a silent skip. It never fences the workdir, the run dir, `/tmp`, site-packages, the model cache or a `data:`/`references:` mount SOURCE (allow-listed even inside an editable tree); it is a PATH fence, not an inode one, because `realpath` per open measured +88 % against the prefix compare's +2.8 %. It is now the MESSAGE rung of a two-rung boundary, not the whole boundary: an audit hook cannot see a read that never reaches Python — measured, `safetensors.torch.load_file` on the incident's own 92 MB checkpoint loads 55 tensors and raises ZERO `open` events, so its coverage of THAT incident is a property of `transformers` reading `config.json` first, i.e. a third-party library's file ORDERING. The kernel rung beside it is `landlock.py` (opt-in), and the hook is not superseded by it: Landlock refuses with `EACCES` → `PermissionError` → an `OSError`, which is exactly the shape this refusal deliberately is not. Also `metric_subject.py` (the METRIC's referent: `eval.metric.subject` names the workdir-relative artifact a number is a CLAIM ABOUT, and the engine binds it at the score stage's start to `atomicio.file_identity` + a sha256, full under 256 MiB and SAMPLED above it with the mode recorded — because a `float` has no referent and, measured across the six repo runs with an event log, 82 of 83 recorded metrics carry NO `metric_provenance` at all while 2 of 83 are provably about bytes the node did not produce. UNBOUND under `metric_subject="require"` gets the EXISTING `metric_salvaged` violation, never a second exclusion vocabulary), `read_allowlist.py` (the ONE derivation of what an eval may read — workdir, run dir, every `data:`/`references:` mount SOURCE, the interpreter, the model cache, the machine tiers — derived from the operator's declarations because 116/116 corpus workdirs read outside their own workdir and 1/116 legitimately distils from a teacher checkpoint INSIDE the editable root; a MACHINE tier absent from the box is dropped at derivation, an absent DECLARED mount is kept so the launcher refuses it by name) and `landlock.py` (the kernel allow-list, `Settings.landlock`, OFF by default: enforcement and +2.1 %/open are verified here but only on `open` microbenchmarks, and nobody has run a ruleset through a real GPU eval — `looplab landlock-check <run_dir>` is the validation path) |
| `looplab/tools/` | agent-facing tools; `_base.py` documents the ToolProvider contract; `env_inspect.py` (repo Developer's read-only env inspector: pkg version/API/source), `vectorstore.py` + `memora.py` (embeddings + harmonic index) |
| `looplab/agents/` | LLM personas: plain roles (`roles.py`), the tool-loop machinery (`tool_loop.py::drive_tool_loop`/`agentic_*`; `agent.py` keeps `run_phase` + `ToolUsingResearcher` and re-exports the rest — patch seams resolve through `agent.py`), external CLI backend (`cli_agent.py`), facade (`unified_agent.py`), and `preflight.py` — the pre-run reachability probe for every role's endpoint, called from `cli/__init__.py::_engine` BEFORE any role is built so an unreachable model refuses the run instead of degrading it to fallback proposals that report success |
| `looplab/search/` | search policies (`policy.py` — action kinds/meta-key constants live here), `operators.py`, best-of-N, surrogate, archive, `panel.py` (the K-idea Researcher wrapper picked by the empirical surrogate), `foresight.py` (hypothesis prioritization / predict-before-execute), `hybrid_merge.py` (grep+BM25+vector RRF retrieval + agent-decided merge, shared by lesson & hypothesis-board consolidation), `proxy.py` (the predictive pre-eval scorer — a policy over folded `RunState`, sibling of `surrogate.py`), `speculation_quality.py` (the paired-run calibration BENCHMARK and its receipt; `speculation_quality_gate` is an ORDERED pipeline of named phases over one `_ReplicateInvariants` record — doc 25 XP-06 — and the order IS the receipt, because `errors` is appended per phase and deduplicated once at the body. Its two cross-pair rules are stated once: `_note_unique` (an identity seen in a second evidence lane; the half that vanishes when hand-written is the RECORD, which makes the check vacuous rather than wrong) and `_replicate_invariant` (the first pair binds, a dissenter never re-binds — the bound values are PUBLISHED and pin a later replay's envelope). A changed derivation revokes every issued receipt and costs six GPU runs, so a change here is proved by comparing `canonical_json(body)` against the previous tree over the same corpus, not by inspection), and the PART IV/V **concept cluster** — five modules in a strict layer order that `concept_graph.py` alone used to be (doc 25 SE-09): `concept_graph.py` (the `Concept`/`ConceptGraph` axis-DAG + the curated task skeletons) ← `concept_tagging.py` (heuristic + LLM taggers; `experiment_nodes`/`node_text` are the two surfaces every tagger describes an experiment by) and `concept_lens.py` (the hierarchy/lens view projections `serve/` and `tools/` read — they stay in `search` because `tools` may not import `serve` and `events` may not import `search`; it also owns `project_concept_map`, the GLOBAL cross-run concept map, which takes per-run concept SETS and nothing else. That signature is the whole point: `portfolio_concept_graph` chose its own population (run-end capsules) while the UI's `Concepts` view chose another (the live run-list rollup), and on the shipped corpus those were 3 runs and 15 — so the capsule reader was removed and the caller now owns both the population and its governance canonicalization. `ui/src/conceptForest.js` is the browser half) ← `concept_analytics.py` (the PURE coverage/metrics/alarm read-models) ← `concept_map.py` (LLM consolidation + `build_concept_map`). There is deliberately NO re-export façade on `concept_graph.py` (a stale spelling must be an ImportError, not a resolving alias), and a cluster module reaches a sibling's FUNCTIONS through the module object — a `from … import <fn>` binds by value and would silently cost the monkeypatch seams the one-file version had. `tests/test_concept_module_split.py` drives both rules |
| `looplab/trust/` | gates that keep results honest: leakage, reward-hack, CV, redaction, confirmation |
| `looplab/engine/` | **the orchestrator loop** + cross-run memory; see invariants below. The `Engine` class spans TWENTY files: `orchestrator.py` (`__init__`, the `run` spine, node creation — the module-global `fold` seam lives here, which is also why the spine's PHASE HELPERS stay in this file rather than becoming a mixin: doc 25 XP-06 shed `_enter_run` (the re-entry prologue), `_settle_terminal_gate` (the settle-then-finish ladder four gates spelled out — NOT to be confused with the module-level `_run_terminal_gate` PREDICATE; `state.paused` deliberately stays inline, it breaks WITHOUT finishing), `_run_spec_gates` and `_handle_no_actions` out of `_run_with_llm_broker`, beside ES-05's `_handle_create_actions`. `_enter_run` and `_handle_no_actions` FOLD, and a `from looplab.events.replay import fold` in another engine file would leave `monkeypatch.setattr(orch, "fold", …)` applying and simply not reaching them — a silent narrowing, not a red test. A helper reports an outer-loop `break`/`continue` it cannot execute as a returned SIGNAL, and that vocabulary is CLOSED: `tests/test_orchestrator_internals.py` resolves it from real `ast.Return` constants, because a typo'd `"brake"` reads as "not break" and turns a stop into another turn. `CreationRunawayCounters` is the loop's two creation-level bounds plus the rule that charges them, module-level so the rule has a truth table instead of being reachable only through a whole simulated spin) + nineteen mixins — `confirm_phase.py`, `ablation.py`, `novelty.py`, `strategy.py` (the Strategist consult/apply/coverage core; the Strategist agent is `agents/strategist.py`), `concept_cadence.py` (the PART IV/V concept subsystem the strategist cadence used to carry: classifier re-tag / consolidation / edges / hypothesis tags / concept-coverage snapshot / run-base seed — on its OWN `concept_retag_every` gate, NOT `strategist_every`), `verifier_tiebreak.py` (R1-c calibrated-verifier metric tie-break — SELECTION machinery, it can move the reported champion), `research_cadence.py`, `eval_stages.py`, `crash_repair.py`, `eval_dispatch.py`, `audit.py`, `resources.py`, `speculation.py`, `evaluate.py`, `node_build.py`, `proposal_cues.py`, `card_reservation.py` (the native-Card RESERVATION/receipt ledger + the two monotonic id allocators — doc 25 ES-01; every `fold` in it goes through `_fold`, which resolves `orchestrator.fold` as a module ATTRIBUTE at call time. The four files that patch that seam all stay GREEN if it binds `replay.fold` directly — measured — so the cost of the direct import is not a red test but a SILENT narrowing: `monkeypatch.setattr(orch, "fold", …)` would stop covering ~1,100 lines of engine. `tests/test_card_reservation_fold_seam.py` is the only thing holding that scope, and it drives it), and the two per-eval live-log watchdogs `train_monitor.py` (LLM health verdict + gated kill) and `asha_monitor.py` (live-curve rank vs finished siblings, with the rank result handed to an LLM judge that owns the stop decision and can only make the gate stricter + opt-in kill; both append only fold-ignored DIAGNOSTIC events and reuse `_evaluate`'s `kill_signal`). In a mixin `self` IS the Engine — grep the engine package before renaming an engine attribute or hunting a method. A TWENTY-FIRST, `shared.py::SharedEngineMixin`, is the home for members that belong to every cluster and therefore to none (`_agent_may`, `_op_span`, `_cadence_due`, plus `effective_researcher_eval_timeout`): the bar is called-from-more-than-one-cluster AND no state of its own, so it does not become a second god-module. The engine package also holds `speculation_gate.py` (the calibrated speculation ENVELOPE — doc 25 ES-01: the ~95-line narrow runtime check and the role-factory guard that used to be closures over twenty `Engine.__init__` locals, plus the frozen `CalibrationRuntime` record naming those twenty inputs once. NOT a mixin: they are free functions taking the engine, which is the point — as closures the envelope could not be STATED, so no test could reach a single rule of it, and `tests/test_speculation_gate_envelope.py` is the per-rule truth table that became possible. Their bodies are the closures' own text; a reworded policy check is a changed policy check and this envelope refuses runs. It also owns `admit_speculation_lane` — the 207-line lane DECISION doc 25 XP-06 moved down out of `Engine.__init__`, which chooses between the calibration bootstrap, the calibrated replay lane and the product lane and stamps the ten `_speculation_*` identities re-entry is checked against. Both the bootstrap and the replay lane must consult the SAME envelope, or replay silently accepts a runtime the bootstrap would refuse), `costs.py` (the durable per-run `llm_usage`/`llm_cost` cost ledger + `.llm-usage-outbox`) an expanded `finalize.py` (the `finalize_step`/`finalization_finished` wrap-up handshake) and `metric_salvage.py` (the SALVAGE decision: a node failed for something other than "no metric", and the operator's OWN declared reader can still find the metric that eval already produced — v5 node 0 trained 76 minutes, printed its score, and was discarded because its manifest named the checkpoint one directory over. DETERMINISTIC rungs only, never a model, because the agent writes the training script and therefore writes the text an extractor would read, which is a route around the protected `score` stage. It is SELECTION machinery: under the `audit` default the node is evaluated and counted (budget, UI, lineage) but carries a `metric_salvaged` violation that keeps it out of `feasible_nodes`, so it can never become champion or be bred from on a value nobody measured. **That gated breeding and not KNOWLEDGE until 2026-08-13**: the cross-run writers read `state.nodes` directly, so a node barred from selection still supplied both sides of an M6 comparison pair and both the `#id op metric=X` evidence rows and the quoted CODE of an M4 skill card — and unlike a champion pick, those claims LEAVE the run into the shared `lessons.jsonl`/`skills/`, where a later run retrieves them as evidence. `metric_unmeasured` states the rule where the row is minted (reading the ROW, not `metric_provenance`, so `select` still admits what the operator accepted), and `memory.py::unreliable_metric_ids` joins it with the trust gate's own `flagged_node_ids` — the identical leak one violation family over. It is deliberately NOT `promotion_eligible_nodes`: a CONSTRAINT-violating node's number WAS measured and its exclusion is a fact about the bound, so it keeps its place in every population. The boundary is between the two CLAIMS, not around the node — no claim about its METRIC, its OBSERVATION kept: `store_concept_capsule` already recorded the concept TAG while withholding the numeric outcome, and `reflect_lessons` now shows the node with its salvage condition and `salvaged_error` and no number, stamped into `ev_ids` so a later re-check that promotes it re-derives the batch. STILL OPEN: `trust/memo_verify.py::finalize_verified_evidence` checks a cited node's LIFECYCLE and not its feasibility or its flags, so a D8 research claim can still ratify `supported` on a salvaged or reward-hacked node into `research_claims.jsonl`. Its `SALVAGE_CAUSE_TRIAGE_ACTION = "salvage_cause_fix"` is a `node_repaired.triage_action` value that is deliberately NOT in `triage.py::TRIAGE_ACTIONS` — a marker, not a verdict; no model may emit it and the coercion must never accept it, which is why the two vocabularies are cross-referenced rather than merged) and its 2026-08-13 sibling `repair_verify.py` (the REPAIR-VERIFICATION rung: did a repair DO what its rationale said? Nothing ever compared the two — `evaluate.py` has said so in a comment since the change-set column shipped ("the rationale says what it INTENDED to change, this says what it did") — and measured over every `node_repaired` in `runs/` (2,477 rows; 2,343 are the 0804 dead-provider boilerplate, 126 of the remaining 134 name something concrete) **38 named a file/flag/parameter/literal that appears nowhere in the diff they produced, and 13 changed NOTHING and still bought a full re-evaluation**. THE ENGINE IS NOT THE PROBLEM and that half of the measurement is what shapes the module: all 198 successful `edit_file`/`write_file` calls in those runs' `spans.jsonl` are present in the `node_repaired.files` the engine committed — the five apparent misses are four superseded by a later write/delete in the SAME repair plus one byte-identical no-op write. So it is a missing rung, not an application bug. `REPAIR_VERDICTS` is a REGISTRY and the tiering is `metric_salvage`'s: `inert` (the change set is EMPTY) is decided on BYTES with the rationale never read, is the ONLY verdict the loop acts on, and two in a row abandon in-node repair (`INERT_REPAIR_LIMIT`, deliberately below `_UNPARSEABLE_REPAIR_LIMIT` — an empty change set is the engine's own proof that the next eval re-runs inputs it already ran); `unmet` (a named claim absent from the diff) comes from a pure extractor over text THE AGENT WROTE, so it is visible-but-not-trusted — it rides on the durable row and into the judge's history and stops nothing. None of the four verdicts is in `TRIAGE_ACTIONS` for the same reason `SALVAGE_CAUSE_TRIAGE_ACTION` is not: a fact the engine derives from bytes is not a judgement a model may emit, and `coerce_triage_action` must reject every one). `memory.py` is no longer the cross-run god-module: `lesson_hygiene.py` (lesson dedup/quality hygiene), `concept_capsules.py` (per-run concept capsules + their validators) and `concept_shelf.py` (the PURE cross-run join that indexes lessons/cases/notes BY the concept tree — the `ATTRIBUTION_RECORD` vs `ATTRIBUTION_RUN` provenance registry, the per-tier coverage receipt, and `build_shelf`, which `serve/routers/misc.py::/api/memory` calls in to; it never calls back out, and it never invents a concept. Its ids are `normalize_concept_id`-canonical, i.e. the per-run ConceptFrame's spelling and deliberately NOT `concept_capsules.jsonl`'s space-preserving `normalize_key` — the two are documented against each other in its docstring because the shelf's reader is an operator who clicked a concept in the run's tree and expects the SAME id to answer here) are its three extracted halves, and two shared rules live beside them — `cadence.py::cadence_due` (the since-last node-count gate, NOT `n % every`, which a fan-out of k > 1 steps clean over — its at_node-idempotence twin is `search/coverage.py::already_covered_at`, parametrized over the snapshot LIST so the two snapshot producers cannot satisfy each other's gate) and `widths.py` (the ONE live concurrency-width settling rule: reject a bool, reject a non-integral float, refuse out-of-range rather than clamp, settle live `0` to serial `1` — live `0` is never AUTO, which belongs to launch-time `Settings`). `lessons.py` shed the same way: `curation_protocol.py` is the FINALIZE at-most-once PAID-curation transaction (claim/recovery, the `.curation_invocations/` scratch, the three steward drivers) — governance money-handling that had nothing to do with lessons, now sitting beside its SIBLING protocol `steward_invocation.py` (the on-demand HTTP/CLI one). The two are deliberately NOT one writer (doc 25 EM-03): finalize is keyed by the SEMANTIC content digest and closes a lost claim itself; on-demand is keyed by the operator's `action_id` and leaves a lost claim OPEN for that operator. They share exactly one table, `governance_health.py::curation_ledger_file`, and `governance_health.py::_validate_curation_row` stays TOTAL over four coexisting row generations because it reads durable history — its docstring names which writer each branch serves and what deleting it would cost |
| `looplab/engine/` | **the orchestrator loop** + cross-run memory; see invariants below. The `Engine` class spans TWENTY files: `orchestrator.py` (`__init__`, the `run` spine, node creation — the module-global `fold` seam lives here, which is also why the spine's PHASE HELPERS stay in this file rather than becoming a mixin: doc 25 XP-06 shed `_enter_run` (the re-entry prologue), `_settle_terminal_gate` (the settle-then-finish ladder four gates spelled out — NOT to be confused with the module-level `_run_terminal_gate` PREDICATE; `state.paused` deliberately stays inline, it breaks WITHOUT finishing), `_run_spec_gates` and `_handle_no_actions` out of `_run_with_llm_broker`, beside ES-05's `_handle_create_actions`. `_enter_run` and `_handle_no_actions` FOLD, and a `from looplab.events.replay import fold` in another engine file would leave `monkeypatch.setattr(orch, "fold", …)` applying and simply not reaching them — a silent narrowing, not a red test. A helper reports an outer-loop `break`/`continue` it cannot execute as a returned SIGNAL, and that vocabulary is CLOSED: `tests/test_orchestrator_internals.py` resolves it from real `ast.Return` constants, because a typo'd `"brake"` reads as "not break" and turns a stop into another turn. `CreationRunawayCounters` is the loop's two creation-level bounds plus the rule that charges them, module-level so the rule has a truth table instead of being reachable only through a whole simulated spin) + nineteen mixins — `confirm_phase.py`, `ablation.py`, `novelty.py`, `strategy.py` (the Strategist consult/apply/coverage core; the Strategist agent is `agents/strategist.py`), `concept_cadence.py` (the PART IV/V concept subsystem the strategist cadence used to carry: classifier re-tag / consolidation / edges / hypothesis tags / concept-coverage snapshot / run-base seed — on its OWN `concept_retag_every` gate, NOT `strategist_every`), `verifier_tiebreak.py` (R1-c calibrated-verifier metric tie-break — SELECTION machinery, it can move the reported champion), `research_cadence.py`, `eval_stages.py`, `crash_repair.py`, `eval_dispatch.py`, `audit.py`, `resources.py`, `speculation.py`, `evaluate.py`, `node_build.py`, `proposal_cues.py`, `card_reservation.py` (the native-Card RESERVATION/receipt ledger + the two monotonic id allocators — doc 25 ES-01; every `fold` in it goes through `_fold`, which resolves `orchestrator.fold` as a module ATTRIBUTE at call time. The four files that patch that seam all stay GREEN if it binds `replay.fold` directly — measured — so the cost of the direct import is not a red test but a SILENT narrowing: `monkeypatch.setattr(orch, "fold", …)` would stop covering ~1,100 lines of engine. `tests/test_card_reservation_fold_seam.py` is the only thing holding that scope, and it drives it), and the two per-eval live-log watchdogs `train_monitor.py` (LLM health verdict + gated kill) and `asha_monitor.py` (live-curve rank vs finished siblings, with the rank result handed to an LLM judge that owns the stop decision and can only make the gate stricter + opt-in kill; both append only fold-ignored DIAGNOSTIC events and reuse `_evaluate`'s `kill_signal`). In a mixin `self` IS the Engine — grep the engine package before renaming an engine attribute or hunting a method. A TWENTY-FIRST, `shared.py::SharedEngineMixin`, is the home for members that belong to every cluster and therefore to none (`_agent_may`, `_op_span`, `_cadence_due`, plus `effective_researcher_eval_timeout`): the bar is called-from-more-than-one-cluster AND no state of its own, so it does not become a second god-module. The engine package also holds `speculation_gate.py` (the calibrated speculation ENVELOPE — doc 25 ES-01: the ~95-line narrow runtime check and the role-factory guard that used to be closures over twenty `Engine.__init__` locals, plus the frozen `CalibrationRuntime` record naming those twenty inputs once. NOT a mixin: they are free functions taking the engine, which is the point — as closures the envelope could not be STATED, so no test could reach a single rule of it, and `tests/test_speculation_gate_envelope.py` is the per-rule truth table that became possible. Their bodies are the closures' own text; a reworded policy check is a changed policy check and this envelope refuses runs. It also owns `admit_speculation_lane` — the 207-line lane DECISION doc 25 XP-06 moved down out of `Engine.__init__`, which chooses between the calibration bootstrap, the calibrated replay lane and the product lane and stamps the ten `_speculation_*` identities re-entry is checked against. Both the bootstrap and the replay lane must consult the SAME envelope, or replay silently accepts a runtime the bootstrap would refuse), `costs.py` (the durable per-run `llm_usage`/`llm_cost` cost ledger + `.llm-usage-outbox`) an expanded `finalize.py` (the `finalize_step`/`finalization_finished` wrap-up handshake) and `metric_salvage.py` (the SALVAGE decision: a node failed for something other than "no metric", and the operator's OWN declared reader can still find the metric that eval already produced — v5 node 0 trained 76 minutes, printed its score, and was discarded because its manifest named the checkpoint one directory over. DETERMINISTIC rungs only, never a model, because the agent writes the training script and therefore writes the text an extractor would read, which is a route around the protected `score` stage. It is SELECTION machinery: under the `audit` default the node is evaluated and counted (budget, UI, lineage) but carries a `metric_salvaged` violation that keeps it out of `feasible_nodes`, so it can never become champion or be bred from on a value nobody measured. The ONE exception belongs to `metric_salvage_repair`, not to that rung: a node whose repaired declaration then PASSES the re-checked artifact contract was measured after all and loses the violation — restricted to OPERATOR-produced output, which is what keeps it from being more permissive than `select`. Its `SALVAGE_CAUSE_TRIAGE_ACTION = "salvage_cause_fix"` is a `node_repaired.triage_action` value that is deliberately NOT in `triage.py::TRIAGE_ACTIONS` — a marker, not a verdict; no model may emit it and the coercion must never accept it, which is why the two vocabularies are cross-referenced rather than merged). `memory.py` is no longer the cross-run god-module: `lesson_hygiene.py` (lesson dedup/quality hygiene), `concept_capsules.py` (per-run concept capsules + their validators) and `concept_shelf.py` (the PURE cross-run join that indexes lessons/cases/notes BY the concept tree — the `ATTRIBUTION_RECORD` vs `ATTRIBUTION_RUN` provenance registry, the per-tier coverage receipt, and `build_shelf`, which `serve/routers/misc.py::/api/memory` calls in to; it never calls back out, and it never invents a concept. Its ids are `normalize_concept_id`-canonical, i.e. the per-run ConceptFrame's spelling and deliberately NOT `concept_capsules.jsonl`'s space-preserving `normalize_key` — the two are documented against each other in its docstring because the shelf's reader is an operator who clicked a concept in the run's tree and expects the SAME id to answer here) are its three extracted halves, and two shared rules live beside them — `cadence.py::cadence_due` (the since-last node-count gate, NOT `n % every`, which a fan-out of k > 1 steps clean over — its at_node-idempotence twin is `search/coverage.py::already_covered_at`, parametrized over the snapshot LIST so the two snapshot producers cannot satisfy each other's gate) and `widths.py` (TWO rules about concurrency, and they answer different questions. (1) the ONE live concurrency-width settling rule: reject a bool, reject a non-integral float, refuse out-of-range rather than clamp, settle live `0` to serial `1` — live `0` is never AUTO, which belongs to launch-time `Settings`. (2) `per_experiment_gpu_budget`, the per-experiment GPU CEILING `engine/proposal_cues.py::_gpu_budget_hint_text` splices into both Researcher prompts — a derivation, not a width settling, and deliberately NOT the same rule as `resources.py::_resource_request_for_node`'s undeclared-footprint branch, which grants a flat 1 device. The two disagree whenever `eval_parallel != pool`; that is a real open gap, recorded in the helper's own docstring, not an equivalence to lean on). `lessons.py` shed the same way: `curation_protocol.py` is the FINALIZE at-most-once PAID-curation transaction (claim/recovery, the `.curation_invocations/` scratch, the three steward drivers) — governance money-handling that had nothing to do with lessons, now sitting beside its SIBLING protocol `steward_invocation.py` (the on-demand HTTP/CLI one). The two are deliberately NOT one writer (doc 25 EM-03): finalize is keyed by the SEMANTIC content digest and closes a lost claim itself; on-demand is keyed by the operator's `action_id` and leaves a lost claim OPEN for that operator. They share exactly one table, `governance_health.py::curation_ledger_file`, and `governance_health.py::_validate_curation_row` stays TOTAL over four coexisting row generations because it reads durable history — its docstring names which writer each branch serves and what deleting it would cost |
| `looplab/adapters/` | task types (toy → dataset → repo → MLE-bench); the TaskAdapter contract is documented in `adapters/tasks.py` |
| `looplab/serve/` | FastAPI server (`server.py` is a thin composition root; routes live in `serve/routers/*` — control/runs/genesis/assistant/boss/org/reports/misc/attention/collaboration/reviews/cross_run), TUI, assistant; the authoritative command lifecycle (`run_commands.py` — the durable record store, the spawn/execution leases, the cross-process sequencer and the `RunCommandService` worker) and, split out from under it (doc 25 SC-01), the HTTP control-payload validator `control_validation.py` — `normalize_control` plus the 35 named per-event rules, where every per-event control rule is a `ControlSpec` field, joined from FIVE tables each asserted equal to `protocol.py::CONTROL_EVENTS` at import: `CONTROL_DATA_FIELDS`/`_CONTROL_NORMALIZERS`/`_CONTROL_PRECONDITIONS`/`_CONTROL_DECISIONS`/`_CONTROL_POLICIES`, plus a cross-check that exactly the `COLLABORATION_EVENTS` declare an append-time precondition. Adding a control event means adding a row to all five — `None` is an explicit "no rule of its own", and a missing row refuses the import rather than inheriting a neighbour's handler. The direction is one-way: `run_commands` imports the validator, never the reverse (the registry guard re-executes the validator as a fresh module, so a back-edge would drag the record store into that probe), and it re-exports ONLY the validator names its own code CALLS — a re-export of anything else is a decoy patch seam that resolves while the validator goes on reading its own global. The one name straddling the cut is `_card_resource_envelope` (intake normalization in the validator, the append-time re-check in `RunCommandService`); `run_commands` reaches it through the MODULE object so one `monkeypatch.setattr("looplab.serve.control_validation._card_resource_envelope", …)` is observed on both sides. `tests/test_serve_module_seams.py` drives all four rules. `assistant_watch.py` (the ALWAYS-ON assistant — a durable STANDING WATCH record under `<root>/assistant/.watches/` plus a lazily-started scheduler over it. Deliberately NOT a `jobs.py` job: that registry is process-local, capacity-capped and evicts a completed receipt after eleven minutes, which is right for "a slow request the browser is waiting on" and wrong for "monitoring that must outlive the process". The record carries the operator's OWN instruction, so a wake-up is re-enterable by a process that did not arm it — that is the whole reason it is durable rather than a timer. Its four collaborators (run observation, turn runner, transcript append, error sink) are INJECTED by `routers/assistant.py`, so this module imports exactly one thing above `core` — `serve/protocol.py`, for the run-phase vocabulary its trigger waits on — and `tests/test_assistant_watch.py` drives the backoff, the terminal ladder and the restart refusal with no HTTP client and no model. Three properties hold doc 36's line by construction and each has a test: the TRIGGER is evaluated by the server over the folded run projection (the agent is woken BECAUSE a state was observed, never because it said one was — an agent that could declare its own wake condition met would be reading a signal it controls); a wake-up runs through the same `run_turn` at the mode PINNED when the watch was armed, so a read-only chat can never wake into a mutating one and the HTTP route takes the mode from the session and never the body; and the module appends no domain event and names no control intent, held by an AST check on its imports rather than a substring pin its own docstring would satisfy. A wake-up may not arm further watches — one standing watch arming another is an unbounded population with a bounded per-watch budget, which is not a bound. On restart a `waking` record left by a dead process is re-armed if it was READ-ONLY and becomes `interrupted` with a stated reason if it could have mutated: its turn may have applied half a change and the trace proving which half is gone. A wake-up also DEFERS rather than interleaving when the operator holds the session's single active-turn slot — costing nothing and consuming no wake-up), Incremental command-ack observation (`command_observation.py`), the owner attention feed (`attention.py`), the isolated reviewer read namespace (`reviews.py`), `scope_report_store.py` (the durable scope-report STORE — paths/receipts/leases/fences/record validation, none of it HTTP; `routers/reports.py` star-re-exports it, so patch the seams HERE, not on `reports.<name>`), `scope_actions.py` (the paid ACTION protocol ABOVE that store — reconciling a claim against its receipt + lease marker + fence + two OS byte-range locks, plus the two `/api/scope-report-actions/…` bodies; it IMPORTS store names, which binds them BY VALUE, so it is a THIRD patch surface — `tests/test_report.py::_STORE_PATCH_MODULE_PATHS` is the sweep and `tests/test_scope_actions_service.py` fails if a module imports from the store without being listed), `run_projections.py` (the run-list projections behind `AppState.run_summaries()`/`run_membership()`, imported BY routers and never the other way round), `router_wiring.py` (the mount ORDER `make_app` used to spell inline, plus `LATE_BOUND_ROUTER_CALLABLES` — doc 25 XP-05: no router may import a router and the destructive/command layers may not import one at all, so a producer's `build_router` assigns onto `srv` and the consumer reads it back at REQUEST time. Drop the producing router and the app still imports, still constructs, still serves every other route, and then either 500s on ONE endpoint naming the consumer rather than the missing router, or — for the three consumers that probe `getattr(srv, "<name>", None)` — SKIPS its step silently. That silent branch is `flush_pending_run_costs`, produced by `routers/boss.py` and read by `deletion_service`/`reset_route`/`run_commands.destructive_guard`: absent, an irreversible reset or delete proceeds with a paid call still awaiting durable run-cost accounting and reports nothing. Its twin `flush_durable_run_costs` fails closed (503) at both its consumers; the asymmetry is real, is driven by a test, and is why they are two rows. `mount_routers` refuses both shapes at construction — live consumer without producer, and producer mounted but no longer assigning. `router_consumers` (live only when mounted) is split from `service_consumers` (imported code, always live) because folding them would let a row whose only readers are services pass a mount omitting its producer — exactly the two boss rows. The scan in `tests/test_router_wiring.py` is keyed on the produced NAMES, never the `_fn` suffix: that convention covers two of the four rows and misses the two that cost money. An EMPTY consumer tuple is a fact, not an omission — `list_runs_fn` has had no production reader since SR-12. Deliberately NOT a shared-services object: every row is a route BODY or a `srv`-bound closure whose promotion to `AppState` would put HTTP and paid-work concerns in the state bag), `durable_op.py` (the RECEIPT tier of the split `core/fence.py` made for the fence MARKERS — doc 25 SC-06: the paranoid read-then-verify load and the validate/immutable/transition/publish/confirm save that `reset_transaction.py` and `deletion_transaction.py` had twice, plus `refuse_unless_quiescent`, the three-probe destructive-quiescence ladder. Each owner declares ONE `ReceiptProtocol`; what it keeps is what must never be flattened — reset's phase lattice is an unordered adjacency table, deletion's a monotonic index with an ABSORBING `quarantine_ambiguous` that must never become resumable, and their caps differ 64 MiB vs 64 KiB. Deletion's `retryable` flag is part of that contract and not decoration: `_pending`'s `true` is a PROMISE that pressing again can move the operation, and exactly one rung could not keep it — a fenced source left non-empty by an interrupted quarantine move answered `409 delete_quarantine_conflict` retryably forever, because nothing in the server ever touched the files blocking it (v5, 2026-08-13: six partial atomic writes the geesefs per-object rename missed, and an operation that owned the run permanently). `_absorb_quarantine_residue` FINISHES that move — each remaining entry is renamed into the SAME operation's quarantine (`atomicio.durable_no_replace_rename(cross_directory=True)`, its only caller), never deleted, never over a name already there, with directories removed by `rmdir` alone so a mid-walk arrival refuses instead of vanishing. A name the quarantine already holds is the proof that an entry is NOT this move's residue, so the original refusal survives intact for the case it was written for — and now answers through `_wedged`: `retryable: false` plus `blocking_entries`. The PHASE does not move for it; a third absorbing state meaning "a human must look" would be a second spelling of `quarantine_ambiguous`. `refuse_unless_quiescent` shares the probe SET and ORDER only: the three refusal vocabularies are live HTTP contracts and stay at the call sites as REQUIRED keyword builders, so a fourth rung cannot ship half-wired. `reject_if_active` is deliberately NOT routed through it — it is a different ladder over `_active_record`/`_unresolved_terminal_record` with an `allow_incomplete_finalize` opt-out no destructive caller may have), the two halves of the serve-side PAID-work boundary — `paid_work.py` (the generation-bound metering/attribution lease) and its per-operation claim→terminal receipt ledger `paid_ledger.py` (doc 25 SR-01: the event fold, the fsync-confirm and the sequenced terminal append that `routers/boss.py`'s report refresh and `routers/runs.py`'s concept lens each hand-rolled). Their fold SKELETONS were near-identical and their meanings are not, so `conflict_policy` is an explicit spec field with no default: report_refresh is `FIRST_TERMINAL_WINS` because its paid success terminal is appended by the worker OUTSIDE the ledger (dropping an unclaimed one re-bills the operator), the lens is `FAIL_CLOSED` and digest-bound because every lens terminal is written THROUGH the ledger (admitting an unclaimed one publishes a lens nobody asked for) — measured, flattening either way changes 204/600 resp. 218/600 randomized folds. The parameterized claim/terminal types are allow-listed in `SERVE_PAID_LEDGER_EVENTS` and asserted at BOTH append sites, so a generic serve-side append helper cannot become the unguarded path around engine invariant #1; the ledger REFUSES to append `report_generated` for exactly that reason. The fsync-confirm seam is `paid_ledger.strict_fsync` and NEITHER router may re-bind that name — a router copy would let `tests/test_report.py`'s injection succeed while reaching nothing (`tests/test_paid_ledger.py` drives all of it). `memory_cascade.py` (what a run's deletion may remove from the five SHARED cross-run stores — one attribution predicate per store, plus the irreversible purge. Keyed on `run_uid`, never the directory NAME, because names are reused and matching on one deleted a different, still-existing run's rows; a legacy log with no `run_uid` degrades to name matching and SAYS SO in the receipt (`identity: "run_id"`). `run_memory_identity` MUST be read BEFORE the run is deleted — both `run_uid` and the per-RUN `memory_dir` live only in the run directory — which is why the receipt echoes both and the retry endpoint takes them in the body. `known_memory_dirs` is the allow-list that keeps that body from naming a store this server was never configured to manage: the purge rewrites whatever it is pointed at, and a containment guard cannot help, since an absolute path to somebody else's store contains no traversal) and `jupyter.py` (the `jupyter_serverproxy_servers` launch spec); never imported by the engine (the run-end projections live in `events/`) |
| `ui/` | React control plane (built artifacts served by `serve/server.py`). The house pattern for anything non-trivial is a **pure model beside its React half**: the transitions/decisions live in a plain ES module with no React and no I/O so `node --test` can drive them directly, and the hook or component keeps only the choreography (timers, aborts, ordering, setState). Existing pairs: `resourceModel.js` / `useScopedResource.js` (`hooks.js` re-exports the hook), `runCommandMachine.js` / `useCommandStatusPoll`, `narration.js` / `Dock.jsx`, and the doc 25 UI-09 pair `runStateModel.js` / `hooks.js::useRunState` (snapshot identity, generation-scoped monotonicity, the owner-vs-review terminal vocabularies, both backoff ramps) plus `traceClearModel.js` / `useTraceClear.js` / `Inspector.jsx::Trace` (the destructive trace clear's confirm→busy→verify/blocked phases; the model owns the one decision that must never be got wrong — "nothing was cleared" vs "outcome unknown, verify this same operation") and `traceScrollModel.js` / `hooks.js::useTraceScroll` / `Inspector.jsx::TraceReach` (how a bounded trace surface reveals its EARLIER steps — a VISIBLE "load earlier steps" button, and scrolling, which does the same thing. The model owns the four states, the one-widen-per-operator-gesture budget that keeps the auto-loader terminating, the stall rule, the window-scaled read deadline, and the rule that a failed widen keeps what the operator had rather than blanking it to `unavailable`; the hook owns only the IntersectionObserver (attached through a CALLBACK ref — the sentinel mounts and unmounts with the state) and the capture-phase scroll re-arm. The reach affordance is a focusable button, VISIBLE at all times (only its focus outline is focus-conditional). It shipped sr-only-until-focused on the reasoning that a pointer user reaches earlier steps by scrolling and should not be shown a control announcing a limit; that was measured against the operator and reversed — they reported the button "gone again", twice, because a control that only exists once you tab to it does not exist to a pointer user. The screen-reader half of the rationale is why it is a real button and not only a sentinel: an IntersectionObserver serves everyone who scrolls but a virtual cursor never scrolls the container. The SERVER ceiling `traceview.TRACE_NODE_SPAN_CAP_MAX` STAYS and is real: measured 2026-08-07 against the live server, `/nodes/{n}/conversation` costs 3.4 ms/span in `SpanIndex.full_spans_for_node` (one random read each, on a geesefs mount) plus 0.9 ms/span in `build_conversation` — 2.2 s at the 512 default and 17.3 s at the 4096 ceiling on rubert-dr-0804 node 1 (14,507 spans, 2,345 repairs), ~64 s / 5.6 MB with no ceiling at all, every bit of it on the request thread. `/nodes/{n}/trace` is a different story and its ceiling protects only the BROWSER — `light_spans_for_node` serves all 14,507 of that node's spans out of the in-memory index in 0.5 ms. What actually hides the operator's steps is NEITHER, and it is two different things (F6, both fixed 2026-08-13). The first was the RENDER caps: `_CONVERSATION_STAGE_CAP`/on the 43-run corpus only 3 runs have a node above the 512 default and only 1 above the ceiling, while the `_CONVERSATION_TURN_CAP` were flat 64/256 scaled by the window, and boundrender cap binds on nodes as small as 258 spans — on rubert-dr-0804 node 1 a 512-span window derived 256 bands and 425 turns and rendered 64 and 105, i.e. the server paid 3.4 ms/span to read evidence and then dropped three quarters oand lif it. They are now the window's own arithmetic BOUND (`conversation_render_caps`: ≤1 band and ≤2 turns per span, because neither can exist without one), so what the window read is what the operator reads. Re-measured capped vs uncapped at the same window: `build_conversation` 0.17 s vs 0.18 s at x1 and 1.42 s vs 1.39 s at the x8 ceilting — **0 ms**, as recorded; it costs only bytes, 193 KB -> 778 KB at x1 and 1.56 MB -> 6.2 MB at x8. The second was that the window is a TAIL and could only GROW: on the 43-run corpus only 3 runs have a node above the 512 default and only 1 above the ceiling, but that one node is 14,507 spans over 3 h 50 m, of which the default window shows the last 7.6 minutes and the ceiling the last 59.3 — 74 % of it unreachable at any limit. Do NOT reach for the ceiling; the window now MOVES. `?before=<span_id>` on both per-node routes reads the same `limit` spans ENDING at a chosen step (`SpanIndex._anchored`, material in both the index's window revision and the route's ETag so no conditional read answers one anchor with another's body; an unplaceable anchor is a 409, never a silent tail), and `/nodes/{n}/episodes` names the places worth pointing it — every band the conversation reads with none of their contents, derived by `_conversation_bands` ITSELF from the in-memory light index, 7,048 episodes in 82 ms and zero spans.jsonl bytes. Its browser half is the pair `traceEpisodeModel.js` / `Inspector.jsx::TraceEpisodes`, VISIBLE beside the attempt picker whenever the node's projection reports omitted spans, stepping by ordinal because 2,345 rows is a phone book. **The two pickers are not substitutes and the trap is worth knowing: an HATTEMPT is a li at a fecycle generation, bumped by `node_reset` ONLY — inline repair does not bump it**, so all 2,345 of that node's repairs are attemptixed span window costs 0 and the attempt picker does not render at all on the ms of servery node the complaint was about (`traceProjection.js` and `docs/guide/ui.md` both asserted the opposite until F6 measured it) time, measured. **Neither is what makes a trace read SLOW, either.** Re-measured 2026-08-12 against the live server on `runs/rubertlite-dr-unified-v5` while its engine was running: `/nodes/1/trace?limit=512` took 4.3-15.6 s to return **1.4 KB describing SIX spans**, and the conversation twin 20.4-22.3 s — with ~50 ms of CPU per request. The cost is the ABSENT-FENCE probes each trace route makes (five for the node trace: `AppState.run_dir`'s deletion fence, plus the reset marker in `_assert_trace_reset_clear` and `_state_payload`, once each for the before/after lifecycle CAS). On the geesefs/S3 mount a run root usually lives on, an `lstat` of a file that is NOT there costs 105-950 ms while one of a file that IS there costs 0.4 ms — and a live run's own writes keep invalidating the directory listing that would answer it, which is why a FINISHED run is fast on the second read and the live one never is. `core/fence.py::_warm_directory_lookup` now issues one `scandir` (1.4-1.8 ms) before each probe, taking those five from a 1,714 ms median to 11 ms. It is a PREFETCH: the `lstat` still decides, the listing is never consulted, and `tests/test_fence_protocol.py` drives both halves. So the client deadline is a SUM, not a product (`traceScrollModel.js::traceReadDeadlineMs` = a fixed per-request term + a window term); the flat 8 s it used to be was aborting six-span reads, and every such abort is a "Trace unavailable" panel. That module also owns the failure VOCABULARY the surfaces print: a SUPERSEDED read (the server answered about a generation/node/attempt the fence refused) is not an unreadable one, and `useTraceRetry` (hooks.js) gives the one-shot readers — an event row's node trace, an operation trace, a finished node's conversation — the bounded automatic recovery the polling ones always had) and `conceptForest.js` / `PortfolioConcepts.jsx` (the run list's GLOBAL concept view — the model folds the scoped run rows' `concepts` rollup into a FOREST of `/`-path trees and owns every honesty rule: distinct-run subtree counts rather than a sum over tags, no subtree experiment TOTAL because one experiment carries several tags, a best metric only where `runIndex.js::metricComparable` holds, an always-present Untagged bucket, and separate roots for runs that disagree about the hierarchy — `spellingVariants` REPORTS `-`/`_` drift and never merges it, because merging would assert a taxonomy nobody authored. Its `{roots, nodes:{parent, depth, children, tagged}}` mirrors `search/concept_lens.py::project_hierarchy` field-for-field so the global and in-run trees speak one vocabulary. `buildConceptCooccurrence` is the map's ONE non-derivable relation — the tree IS `is_a`, so what no id can state about itself is which concepts a run named TOGETHER: an unordered pair weighted by DISTINCT runs, with a floor (default 2) so a single-run coincidence is not an edge, and the below-floor candidates COUNTED so an empty panel cannot read as "nothing co-occurs". It mirrors `concept_lens.py::project_concept_map`, which is the same fold over whatever population its caller scoped — the agent tools pass capsules, this passes run rows. `PortfolioConcepts` takes its runs as a PROP — the same array the Lineage view draws — so scope is shared by construction and the fold costs nothing on the list's 2.5 s poll (measured on the shipped 46-run corpus: forest 4.1 ms, co-occurrence 0.54 ms). Neither half fetches: `engine/concept_capsules.py::portfolio_concept_graph` was the second implementation of this same 'global concept map' and was REMOVED rather than routed, because its corpus is the run-end capsule ledger — 3 rows against the list's 15 tagged runs on that corpus — and an unscoped cross-run payload rendered inside a filtered view misstates the population). `claimsCurationModel.js` / `ClaimsCuration.jsx` is the same pair for the **Claims & Curation** screen at `#/claims` — the ex-Research Atlas, renamed by doc 29 F7 after the three sections it UNIQUELY holds (every claim record, the mixed-evidence subset, and the paid stewards' proposals and outcomes) once its fourth, a concepts list, was dropped. That list was a weaker copy of `PortfolioConcepts`, and the OLD NAME was the actual defect: it made an operator expect the concept map and then find the worse one, so the rename is the fix and not decoration. The header links to `#/concepts`, a deep link App resolves to `{view: 'list', listView: 'concepts'}` — RunList captures `initialNavigationState` exactly once, so the requested view travels as an initial snapshot AND re-keys the list; a memoized object is required there because an unstable identity re-fires the list's own restore/focus effects every render. **The HTTP contract did not move with the surface**: `/api/cross-run/atlas` still serves the mixed-evidence records (its one production reader is this client), `/api/cross-run/claims` still serves the ledger, `looplab atlas` is untouched, and the model's `atlas` SOURCE KEY keeps the endpoint's spelling on purpose — a key that no longer names its route sends the next reader hunting for a route that does not exist. `#/atlas` and `#/research-atlas` are canonicalized aliases, and `ui/test/globalNav.test.js` pins that the menu emits neither. What the client stopped keeping is real: the atlas envelope's `explored`/`thin_coverage` and the concept-capsule receipt are out of `api.js`'s `CROSS_RUN_STATE_FIELDS` allowlist, so the "Evidence source incomplete" notice can no longer fire on a store this screen does not touch. `AssistantBar.jsx` is the largest instance and sheds this way rather than by moving stateful React around (doc 25 UI-05): `assistantShareModel.js` (public-link mutations — a snapshot needs a complete turn, a revoke never does; only a 4xx is authoritative, everything else leaves the link UNCERTAIN and must mark the chat unknown), `assistantSessionModel.js` (the late-read fence three awaits used to spell out longhand, the delete-refusal order, the delete-failure table), `assistantChromeModel.js` (the controls the three views share — a gate, its tooltip and its action come from ONE branch), beside the earlier `assistantCommand`/`assistantDirectPolicy`/`assistantErrors`/`assistantPermission`/`assistantRecovery`. Its three layouts stay render functions in the component, NOT sibling components: measured 2026-08-05, they read 136 distinct closure names (80/42/83, only 18 shared), and a props bundle that size has no registry guard — a missing prop arrives as `undefined`, which for `disabled={…}` silently ENABLES a mutation gate. **AssistantBar's test coverage is almost entirely source pins over the file's TEXT**, which cannot see a JSX structure break: a dropped brace once left `vite build` refusing the tree while all 767 tests passed. `test/assistantBarResourceTruth.test.js` now SSR-loads the module (`vite.ssrLoadModule`) so it at least has to compile — nothing in the suite MOUNTS it. Note `api.js` is a BARREL over `apiClient.js` / `commandModel.js` / `commandProtocol.js` / `commandStorage.js`: the only direct member edge outside that family is the two-name `reviewRouteApi.js` facade used by App, because making the review gate import the whole barrel eagerly loaded every command/storage/SSE protocol before a route existed. `test/apiBarrel.test.js` derives the barrel surface and pins that sole narrow exception |

## Engine invariants (violating these breaks replay/resume)

1. **The engine is the sole writer of domain events.** Background tasks return values; only the
   main task appends FOLDED events — with ONE typed exception: the concurrent-research task may append the
   selection-neutral FOLDED types in `events/types.py::BACKGROUND_APPENDABLE` (asserted at the append
   sites; `tests/test_background_appendable.py` proves splice-position neutrality). A concurrent task
   MAY additionally append `DIAGNOSTIC_EVENTS` (fold-ignored — the fold never reads them). **That is
   NOT the same as splice-neutral, and this invariant claimed it was until 2026-08-06.** The fold is
   not the only reader of the log: `engine/speculation.py::_proposal_authority_seq` fences a paid
   proposal by comparing a max-seq for EQUALITY across the window in which `_prepare_node_idea` makes
   the Developer call, and it excluded only the two LLM-accounting rows. So a `train_monitor_alert`
   or `asha_rank` — both ON by default, both fired from concurrent evals on a TIMER, i.e. landing in
   that window as a matter of course — moved the fence and discarded a proposal the run had already
   paid for, reported as "a control/research/lifecycle event won the CAS". The fence now excludes
   `DIAGNOSTIC_EVENTS` wholesale and `tests/test_card_speculation_engine.py` drives it from the
   registry, so a diagnostic type added later inherits the property. It excludes exactly ONE FOLDED
   pair, `SETUP_THREAD_APPENDABLE`, and only on evidence: those two rows are written from an eval
   WORKER THREAD, so since the eval task group became run-scoped (backlog F1f) they are the only
   authority-bearing rows that can land inside a main-task reservation's CAS window — every other
   concurrent writer is an anyio task on the loop that reservation is blocking — and they are the
   only folded pair whose splice-position neutrality this repo has PROVEN. That is not a precedent
   for `node_evaluated`: a node terminal moves `best`, the parent snapshot and every Card score, and
   it stays in the fence. When you add a non-folded
   event, the question to ask is not "does the fold read it?" but **"does any reader key on its
   position?"**: the training-monitor task appends `EV_TRAIN_MONITOR_ALERT` this way under
   `_write_lock`, asserting membership in `DIAGNOSTIC_EVENTS` at its append site.
   *An UNLOCKED main-task append* is the third accepted shape, and the one that is deliberately NOT in a
   registry: `EV_NODE_EVAL_STARTED` (`engine/evaluate.py::_record_eval_start_boundary`, called by the MAIN
   task from `engine/speculation.py`'s admission decision and again as a funnel backstop in `_evaluate`).
   It is FOLDED but appended outside `_write_lock`. That is safe because it is ONE independent per-node
   row the fold keys by `(node_id, generation)` and applies SET-ONLY, AND because both writers are on the
   main task *after* that node's own `node_created` — not because its position is immaterial. This row is
   NOT splice-neutral, and saying it "pairs with nothing, so its splice position cannot change any other
   event's meaning" (as this invariant did until 2026-08-05) is false: `_on_node_eval_started` silently
   drops a row whose node does not exist yet, and `Node.eval_started` is one of the durable facts
   `core/models.py::is_unevaluated_speculative_discard` proves the Layer-5 budget refund from — which
   `node_counts_toward_card_budget` reads, which BOTH the L3 budget and the fold's debug anchor
   (`events/card_ledger.py::_card_debug_leaf_children`) read. So splicing it before rather than after its own
   `node_created` measurably flips a DIFFERENT Card's `selection_ready`: measured
   `{budget 2, leafs [2,3], later Card ready}` vs `{budget 3, leafs [3], not ready}`. Not reachable
   today (both writes are main-task and node-created-first), which is exactly why it has to be written
   down as an ordering PRECONDITION rather than left as a property of the event.
   It has no registry because the constraint it
   must satisfy is the opposite of `BACKGROUND_APPENDABLE`'s — placement is load-bearing in the other
   direction: writing it from the eval WORKER lands it inside `_request_card_build`'s tail-CAS window and
   makes every prefetch election lose its CAS (measured: depth-1 speculation silently went serial, 17
   builds / 5 discards became 12 / 0). Keep it on the main task, at the dispatch decision.
   *Evaluation children outlive the turn that admitted them* (backlog F1f, 2026-08-13) and this is a
   LIFETIME, not a writer: the eval task group is owned by `Engine.run`, so `_run_card_session`
   returns while its evals burn and the next session adopts them from `Engine._eval_inflight`. It
   needs no exception here because eval children are anyio tasks on the engine's own event loop (not
   threads), every one of the eight node terminals in `engine/evaluate.py` is lexically inside
   `async with self._write_lock`, node creation still commits from the main task in
   `_card_phase_serve_head`, and `_record_eval_start_boundary` still runs where the paragraph above
   says. What it DOES change is what "quiescent" means for a main-task decision: the log being still
   is no longer enough, because a GPU may be mid-training. `_refuse_finish_over_adopted_evals` makes
   every finish CAS refuse over a running eval and `_drain_adopted_evals` pays the wait where it is
   free; the outer loop's `_drop_stale_speculation` and the AUTO-depth ratchet both read
   `_eval_inflight` for the same reason. Ask that question — "does this decision assume no eval is
   running?" — of anything new on the main task.
   *Thread-side setup* is the remaining typed exception: `_ensure_run_setup` (`engine/eval_dispatch.py`)
   appends the FOLDED `events/types.py::SETUP_THREAD_APPENDABLE` pair from an eval WORKER THREAD
   (`_run_eval` under `anyio.to_thread`), outside `_write_lock`. Safe because `EventStore.append`
   serializes bytes, `_run_setup_lock` makes the section once-per-run, and the fold keys
   `run_setup_open`/`run_setup_done` purely BY COMMAND; membership is asserted at both append sites and
   `tests/test_setup_thread_appendable.py` proves splice-position neutrality. UI/CLI append
   only *control intents* (allow-listed in `serve/protocol.py::CONTROL_EVENTS`, enforced by
   `serve/routers/control.py`).
   *Concurrent build fan-out* (canonical `llm_parallel`; legacy `parallel_build`) is a further
   accepted seam: `_create_node` runs
   in worker threads (`anyio.to_thread`) that append their own node's FOLDED events (`node_created`,
   `node_failed`, per-node audit). This is safe because each thread writes an INDEPENDENT node's
   events, `EventStore.append`/`read_all` serialize via their own `threading.Lock`s, ids are reserved
   serially under `_id_lock` up front, and the fold is order-tolerant across independent nodes — so
   only the log's byte-order (not the folded state) becomes nondeterministic. A settled build width
   of `1` keeps the strict "only the main task appends" behaviour, byte-identical. The seam is
   OWN-NODE ONLY: a worker that needs a run-GLOBAL gate (the developer-crash / build-crash
   auto-pause) calls `_request_create_pause` and the MAIN task appends the `pause` after the join,
   via `_drain_create_pause` — a worker-written EV_PAUSE would race a concurrent EV_RESUME for byte
   position, which the fold is not order-tolerant across.
2. **Exactly one terminal event per node** (`node_evaluated` | `node_failed`). The fold is
   idempotent on duplicate terminals (first terminal wins).
3. **Every side effect must be gated on a domain event** so resume-by-replay is idempotent
   (`fork_done`, `inject_done`, `confirm_done`, `<x>_requests`/`<x>s_done` counter pairs).
4. **State is only observed via `fold(store.read_all())`** — never cache derived state across
   loop iterations without re-folding.
5. **`fold` must stay deterministic and order-tolerant**: no I/O, no LLM calls, unknown event
   types are ignored (forward compat), new event data fields are additive-only with reader-side
   defaults for old logs.
6. Settings recorded in the `run_started` event win over live config on resume.
7. Event type names are constants in `looplab/events/types.py`; a typo'd literal silently
   no-ops (unknown types are skipped), and `tests/test_event_types.py` guards against that —
   always add new event types to the registry.

## Conventions and traps

- **Back-compat import shim**: `looplab/__init__.py` aliases every pre-split flat module path
  (`looplab.orchestrator` → `looplab.engine.orchestrator`, …) via a meta-path finder; both names
  resolve to the SAME module object, so monkeypatching either path works. Many tests use old flat
  paths — keep the `_LAYOUT` map in sync when MOVING modules, and `looplab/__init__.py::_RENAMED`
  (old FULL path → canonical full path, checked FIRST) when RENAMING one: `_LAYOUT` maps a canonical
  module STEM to its package, so a renamed module's old name is no longer a stem anywhere and both of
  its old spellings (dotted + flat pre-split) have to be routed explicitly. Both go through the same
  `_CompatLoader`, which is the point — these modules are patch seams, and a second module object
  would make every existing monkeypatch a silent no-op instead of an error.
- **Layering**: `core` imports nothing above itself; `events` only `core`; `serve` may import
  anything; the engine must not grow new dependencies on `serve`. **`search` may import `agents` at
  module level; `agents` may reach `search` ONLY through a deferred (function-local) import.** That
  asymmetry is what keeps the cycle open — five search modules import `agents` at module scope, so a
  module-level `looplab.search` import in `agents/` closes the loop into an ImportError at startup.
  Guarded by `tests/test_agents_search_direction.py`.
- **Comments are load-bearing.** The codebase documents *why* (ADR references, review provenance,
  replay-safety notes) inline. Preserve comments verbatim when moving code; write the same style.
- **Prompt strings are contracts.** Changes to prompt text alter agent behavior — never "clean up"
  prompt wording as part of a refactor. Several prompts are routed through the PromptStore
  (`render(prompts, key, default)`); grep for `render(` to find overridable prompts.
- **An HTTP-contract change must move its tests in the SAME change.** Changing a route's verb,
  path, required params, response shape, or refusal codes silently retires every test that still
  speaks the old contract: the request 404s, 422s, or raises `ImportError` on a renamed private,
  and the assertion below it never runs. This is worse than a red test, because the properties
  these tests guard are disproportionately the security ones — token gating, share allow-listing,
  provider-error redaction, path-traversal allow-lists. A single day of contract hardening left
  23 such tests stranded across `test_server`/`test_assistant_*`/`test_attention`, and every one
  read as "a regression" when the production code was fine. When you change a contract: grep the
  suite for the old spelling, re-point it, and RE-VERIFY the property still holds rather than
  making the test green. If a refusal code now depends on a race, pin the fail-closed SET, not one
  member.
- **Duck-typed seams are REGISTRY-GUARDED** — a rename that used to break silently is now a red
  test. The registries (each with a two-way source-scan test): TaskAdapter hooks
  `adapters/tasks.py::TASK_OPTIONAL_HOOKS`; role outputs `agents/roles.py::DEVELOPER_OUTPUT_ATTRS`
  / `RESEARCHER_ACTION_ATTRS`; hint attrs `agents/roles.py::RESEARCHER_HINT_ATTRS`; prompt keys
  `core/prompts.py::PROMPT_KEYS`; delivered signals `engine/signal_delivery.py::SIGNALS`;
  crash-triage verdicts `engine/triage.py::TRIAGE_ACTIONS` / `AGENT_TRIAGE_ACTIONS` (the
  inline-repair STOP decision — a typo'd literal turns a stop into "keep repairing");
  repair-verification verdicts `engine/repair_verify.py::REPAIR_VERDICTS` (what a repair DID to the
  tree, written to `node_repaired.verified`, read back by `_durable_repair_ledger` and rendered by
  `_format_repair_log` — a typo'd literal turns "this repair changed nothing" into "no opinion" and
  restores the unbounded inert chain it bounds. Its members are deliberately DISJOINT from
  `TRIAGE_ACTIONS`, which the guard asserts in both directions: `node_repaired` now carries a
  `triage_action` AND a `verified`, and one is a model's judgement while the other is the engine's
  byte comparison);
  metric-reader path slots `runtime/command_eval.py::READER_PATH_KEYS` (which spec keys each reader
  turns into a filesystem path — deliberately NOT the same table as `READERS_REQUIRING_PATH`, since
  "needs a path" and "names a path" are different facts: `_read_adapter` DEFAULTS its path and still
  builds one from `spec["path"]`, and `_read_host_score` names TWO files under other key names, one
  of which reaches `Path(...)` without going through `_confined`. A reader added with an
  unregistered path slot is one `Path(workdir) / 123` away from an uncaught TypeError that takes
  down the RUN — no node terminal, re-dying on every resume. `tests/test_metric_reader_confinement.py`
  drives the registry AND re-derives it from each reader's own `spec.get` reads, so a new reader
  goes red rather than silently escaping the table; `spec_kind` is the same rule for the DISPATCH,
  where an unhashable `kind` crashed the submit-time refusal itself);
  background-appendable events `events/types.py::BACKGROUND_APPENDABLE`, its thread-side sibling
  `SETUP_THREAD_APPENDABLE`, and its Card-conditional extension
  `NON_CARD_SELECTION_BACKGROUND_APPENDABLE` (legacy Hypothesis/Policy selection ONLY — the call site
  must prove Card-driven selection is off, because `hypothesis_merged` became a Card
  ownership/lifecycle input and is not universally neutral); LLM retry handlers
  `core/llm.py::OpenAICompatibleClient._RETRY_POLICY` (a `(exception types, handler)` table dispatched in
  order — the same order as the `except` ladder it replaced, so reordering it changes behaviour);
  the build-width LLM markers `agents/roles.py::LLM_PRESENCE_ATTRS` and the `UnifiedAgent` per-stage
  descent `agents/roles.py::FACADE_STAGE_ATTRS` (`_build_calls_an_llm`'s probes — the other half of
  the same AUTO width decision `gpu_capable` is registered for, and the half whose absent marker
  means "no LLM", i.e. a CHANGED treatment rather than the historical default: making the facade's
  public `researcher` handle private silently pinned `llm_parallel` 4 -> 1 and `speculation_depth`
  4 -> 0); capped-LLM-lane producers `core/llm_broker.py::BACKGROUND_LANE_PRODUCERS` (a lane name is
  a bare string, so this is what stops a FOREGROUND producer from joining a lane capped at one
  concurrent request because its prompt looks like background work — the per-eval inter-stage check
  did exactly that and serialized every concurrent eval's stage gate); the tool-loop keyword split
  `agents/loop_options.py::LOOP_OPTION_FIELDS` + `EXPLICIT_ONLY_LOOP_ARGS`, which must PARTITION
  `drive_tool_loop`'s keyword-only parameters — an option travels only inside a `LoopOptions` bundle,
  a per-call callback (and the two prompt contracts `nudge_prompt`/`stuck_prompt`) only as an
  explicit keyword. Adding a parameter to exactly one of the two lists is how the original defect
  comes back: a name reachable BOTH ways raises a duplicate-keyword `TypeError` that the loop's own
  containment `except` swallows, and the agentic Researcher silently degrades to non-agentic in the
  DEFAULT config. Also the engine's sub-object forwarding seam
  `engine/orchestrator.py::Engine.FORWARDED_SUBOBJECT_MEMBERS` (name -> sub-object + `@in_llm_lane`);
  a delegator whose lane is forgotten does not fail, it just runs outside the capped enrichment lane
  and competes with foreground work for provider concurrency, which reads as an unexplained stall.
  Adding/renaming any such seam means updating the registry in the SAME change. Note `search/foresight.py`'s
  panel is a `__getattr__` proxy over the wrapped agent: a typo'd read silently resolves to the
  base object, and an attribute SET on the panel shadows reads *through the panel* but does NOT
  reach the base's own `self.<attr>` reads until `forward_hints` mirrors it — set hints on the
  outermost wrapper and let the registry forward them, never on `base` directly.
- **A deliberate refusal is a TYPE, not a message.** `core/errors.py::OperatorRefusal` marks every
  exception LoopLab raises *on purpose* about the operator's own input (`ConfigRefusal`,
  `EnvironmentRefusal`, `LLMError`, the `RunStartPinError` family); `cli/__init__.py`'s
  `_RefusalBoundaryGroup` prints those as one message at exit `REFUSAL_EXIT_CODE` (2) and lets
  everything else keep its full traceback at exit 1. Raising a BARE `ValueError`/`RuntimeError` for
  a new refusal silently puts it back in the 42-lines-of-frames presentation this split removed —
  and widening the boundary to catch bare `ValueError` instead is the worse failure, because a bug
  reported as a tidy one-line message looks handled. The bar for wearing the marker is in the
  `OperatorRefusal` docstring; `tests/test_cli_refusals.py` pins both halves, control included.
- **Tool providers**: the `bind_state` hook is OPTIONAL (`tools/_base.py` — providers that don't
  need run state simply omit it), but a provider that DOES implement it must accept the second
  `parent` argument (`bind_state(self, state, parent=None)`) or it raises `TypeError` at dispatch.
- **Tests isolate the environment** (`tests/conftest.py`): dotenv loading is disabled,
  `LOOPLAB_MEMORY_DIR`/`LOOPLAB_KNOWLEDGE_DIR` point at tmp dirs, and the host GPU-pool lease is
  redirected to a per-test file. Engine tests construct `Engine(...)` directly (~170 call sites) —
  keep its keyword API stable. New tests should use `tests/factories.py::make_engine` (the one
  canonical construction: toy task + its roles + a subprocess sandbox + `GreedyTree`, everything else
  passed through as keyword overrides). It is deliberately NOT a fixture — resume/crash tests build a
  second Engine over the same run dir — and migration of the existing call sites is opportunistic.
- **A guard test must not be satisfiable by a COMMENT.** Roughly 200 assertions in the suite read
  production source (`inspect.getsource` / `read_text` + `in` / `.index()`), split about evenly
  between POSITIVE pins (`assert "<literal>" in source`) and negative ones — measured 97/96 across 63
  files on 2026-08-06 by an AST scan for `assert "<lit>" [not] in <name-bound-from-source>`. Treat
  those as a magnitude, not a census: the exact figures have drifted every time anyone re-counted,
  partly because reasonable methods disagree on what counts as a pin, so do NOT reason from them and
  do not trust a claim that "all N are non-vacuous" — nobody re-derives it. Re-run the scan if you
  need a number. Every positive pin is one comment away from vacuous, because the cheapest mutation
  is *delete the code, leave a comment carrying the pinned literal*. Full call EXPRESSIONS
  and their ORDER are not enough either: `pass  # self._record_eval_start_boundary(chosen)` satisfies
  all three `source.index()` lookups in `test_card_speculation_engine.py:1731-1733` **in order**
  while the boundary event is never written — the defect whose cost is recorded in invariant 1
  (17 builds / 5 discards became 12 / 0; the budget refund reads `eval_started`). Reach for these in
  order, best first:
  1. **Drive the property.** A real fallback with a counting accountant
     (`test_llm_stream_setup.py`), a real socket that goes silent mid-body
     (`test_llm_streaming_surface.py`), an `lstat` that under-reports a file's size
     (`test_fence_protocol.py`), a real run whose event log is then read
     (`test_trust_gates_reach_the_ledger.py`). Only this kind survives a refactor of the code it
     guards, and only this kind distinguishes "the call happened" from "the effect landed".
  2. **Make the rule statable.** When the property is buried where no caller can reach it — a money
     rule in a `finally` whose guard clause no call site exercises yet — HOIST it into a named
     function and test its truth table (`core/llm.py::_stream_envelope_is_billable`). A rule nobody
     can state is a rule nobody reviews.
  3. **AST, never substrings**, for the residue: that a call is PRESENT, that two guards appear in
     the right ORDER. `tests/_source_scan.py::called_names` / `names_read` / `function_tree` resolve
     real `ast.Call` / `ast.Name(Load)` nodes, and comments are not AST nodes.
     **Know what this tier does NOT prove**, because it is routinely over-read: `called_names` walks
     the whole function tree (nested `def`s included) and sorts by source position, so it shows the
     call exists in the TEXT — not that it executes. A call behind a dead `if`, after an early
     `return`, or inside a nested function nobody invokes all count as present; and "A before B" is
     textual order, so two calls in opposite branches of one `if/else` report an order that never
     happens at runtime. It is also blind to a decorator that wraps or replaces the function, since
     the body it parses is still the original. When the property is really "this ran", tier 3 is not
     enough — go back to tier 1 and observe the effect.
  NEGATIVE pins (`assert "x" not in source`) stay substrings on purpose — what must not
  come back is the TEXT, and a commented-out copy of a re-derivation is as much of a drift risk as a
  live one. Finally, a source pin is not automatically what protects a property:
  `test_background_appendable.py:156-157`'s pins are evadable, but the property IS covered
  behaviourally by `test_deep_research_loop.py` / `test_research_attempt_settlement.py`. Check what
  actually holds the line before rewriting a pin — and re-verify by MUTATING a throwaway copy of the
  tree (`git archive HEAD | tar -x -C /tmp/...`), never the real one.
- **The host GPU-pool lease is ONE file per OS user** (`/tmp/looplab-gpu-pool-<uid>.lock`,
  `engine/resources.py`) and is exclusive ACROSS PROCESSES on purpose. So a GPU-owning run waits for
  every co-hosted GPU-owning run — potentially for hours — and that wait used to be completely
  silent, which reads as a deadlock and repeatedly got debugged as one. It now logs the lease path
  and the holding PID at WARNING. Work that needs no GPU must not enter that queue at all: an
  UNSPECIFIED footprint resolves against the TASK (`adapters/tasks.py::gpu_capable`, absent means
  capable) as well as the box, so the offline/synthetic adapters never take the lease. If a run
  genuinely stalls before its first eval, check that lock before suspecting the engine.
- Settings are flat on purpose (`LOOPLAB_<FIELD>` env vars map 1:1); never nest or rename fields —
  snapshots and env compat depend on the names.
- `looplab/sweep.py` is NOT a CLI subcommand — it is a runtime helper imported by *generated*
  solution code inside the sandbox (see its docstring).
- A run directory contains `events.jsonl`, `config.snapshot.json`, `task.snapshot.json`,
  `engine.lock`, and per-node workdirs (`docs/guide/concepts.md` is accurate;
  `docs/04-file-layout.md` is the original *design* and differs from what shipped).
